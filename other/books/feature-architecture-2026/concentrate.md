# «Фича-ориентированная архитектура фронтенда 2026+ (на примере Angular)»  
Полное, цельное и окончательное издание (ноябрь 2025)

## Введение  
Фронтенд прошёл путь от jQuery-скриптов до полноценных инженерных систем. В 2025–2026 годах старые подходы (толстые SPA, глобальный Redux/NgRx, структура по типам файлов) окончательно умерли. На их место пришли:

- Server-first (SSR/SSG + partial hydration)  
- Fine-grained реактивность (Signals)  
- Feature-oriented вертикальные срезы  
- Чёткое разделение четырёх уровней архитектуры  

Эта книга — не про «ещё одну структуру папок», а про системное мышление, которое позволяет проектам жить 5–10 лет без полной переписки.

## Ключевые тезисы всей книги (концентрат)

### 1. Четыре уровня архитектуры (фундаментальная модель 2025+)
1. Структура проекта (папки, фичи, core/shared)  
2. Прикладная/доменная архитектура (модели, use-cases, фасады)  
3. Внутренний дизайн фичи (UI → Facade → Store → Domain/Data)  
4. Runtime/Delivery (SSR, SSG, hydration, @defer, edge)

Никогда не смешивать слои — это главная причина деградации проектов.

### 2. Структура проекта — только feature-oriented
```
src/app/
├── core/          ← глобальная инфраструктура, глобальный store (<20 полей)
├── shared/        ← чистый, stateless UI + утилиты
├── features/      ← 95 % кода проекта
└── app.routes.ts  ← единственная карта продукта
```

Никаких src/components, src/services, src/pages в корне — это антипаттерн.

### 3. Фича — основная и единственная единица декомпозиции
Фича = автономный вертикальный срез со всем необходимым:
- pages + routes  
- ui (dumb-компоненты)  
- facade (публичный API)  
- state (SignalStore)  
- services/use-cases  
- model (доменные сущности)  
- data/api (репозитории, мапперы)

Каждая фича имеет index.ts и импортируется только через него.

### 4. Поток данных внутри фичи (жёсткий канон)
```
UI (pages + ui) 
  → только Facade 
    → Services / Store 
      → Domain models 
        → Data (API/repositories)
```
Обратный поток — только через сигналы/observables.

### 5. Три уровня состояния (и больше не нужно)
| Уровень       | Где хранить                            | Примеры                              |
|---------------|----------------------------------------|--------------------------------------|
| UI-state      | локальные сигналы в компоненте         | открытые модалки, табы, формы        |
| Feature-state | SignalStore внутри фичи                | корзина, каталог, фильтры, заказ     |
| Global-state  | core/state (только то, что нужно всем) | auth, theme, locale, feature-flags   |

Сервисы НЕ хранят состояние. Глобальный Store < 20 полей.

### 6. Страницы и роуты
- Страница живёт только внутри своей фичи  
- Каждая фича имеет *.routes.ts  
- Корневой app.routes.ts — это карта всего продукта  
- Lazy-loading по фичам — обязательно  
- Страница тонкая: инжектит фасад, использует async pipe, компонует UI

### 7. Runtime — Server-first по умолчанию
- Публичные страницы → SSR или SSG  
- Личные/тяжёлые → CSR + partial hydration + @defer  
- Angular 19+ с hydration и deferrable views делает это тривиальным  
- Цель: ≤ 200–300 КБ JS на первый бандл, LCP < 1.2 с

### 8. Эталонная структура большой фичи (2025+)
```
features/catalog/
├── pages/
│   catalog.page.ts
│   product-details.page.ts
├── ui/
│   product-card.component.ts
│   filters-panel.component.ts
├── facade/
│   catalog.facade.ts
├── state/
│   catalog.store.ts          ← SignalStore
├── services/
│   catalog.service.ts        ← use-cases (applyFilters, etc.)
├── model/
│   product.model.ts
│   category.model.ts
├── data/
│   catalog.api.ts
│   mappers.ts
├── catalog.routes.ts
└── index.ts
```

### 9. Главные антипаттерны (убивают даже идеальную структуру)
- Божественный глобальный Store  
- Компонент-бог (>400 строк, всё внутри)  
- Shared-свалка (бизнес-логика в shared)  
- Псевдо-feature (папки есть, границы нарушены)  
- Сервис со состоянием  
- Прямые вызовы HttpClient из UI  
- DTO в UI и Store

### 10. Эволюция проекта (не переписывать всё сразу!)
Этап 1 (маленький): standalone + сервисы + локальные сигналы  
Этап 2 (средний): фасады + постепенное введение SignalStore  
Этап 3 (большой): полные фичи + строгие границы + server-first  
Этап 4 (огромный): микрофронтенды/пакеты из фич + enforced boundaries

Поднимаем абстракции только когда больно.

### Финальная мысль книги
Код устаревает.  
Фреймворки меняются.  
Способность мыслить четырьмя уровнями архитектуры и держать чёткие границы между фичами — остаётся навсегда.

Сделайте семь шагов прямо сейчас:
1. Перейдите на /features  
2. Введите фасады как обязательный публичный API  
3. Разделите состояние на три уровня  
4. Включите hydration + @defer  
5. Очистите shared и глобальный store  
6. Сделайте роутер картой продукта  
7. Проведите аудит по чек-листу антипаттернов

— и ваш проект мгновенно окажется в 2027 году.

Книга закончена.  
Архитектура получена.  
Теперь — в бой. 🚀