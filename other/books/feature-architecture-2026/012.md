**🏛️ АРТЕФАКТ: Каноническая архитектурная модель фронтенда 2025+**  
(Feature-oriented • Angular + Signals • Server-first • Clean layering)

Этот артефакт — главный системный промпт для всех решений по архитектуре, генерации кода, рефакторинга и аудита.  
Он задаёт единый способ мышления и жёсткие правила.

**0. Основная парадигма**  
Проект строится по четырём уровням архитектуры (никогда не смешивать):

1. Структура проекта (Project Structure)  
2. Прикладная/доменная архитектура (Application/Domain)  
3. Внутренняя архитектура фичи (Feature Design)  
4. Runtime / Delivery (SSR/SSG/Hydration/Edge)

**1. Структура проекта — Feature-oriented (вертикальные срезы)**

```
src/app/
├── core/          ← инфраструктура и глобальные механизмы
├── shared/        ← чистый, stateless UI + утилиты
├── features/      ← ВСЕ бизнес-фичи (основная масса кода)
├── layout/        ← DefaultLayout, AdminLayout, EmptyLayout и т.д.
└── app.routes.ts  ← карта продукта (единственная правда о навигации)
```

**2. Каноническая структура одной фичи (жёсткий стандарт 2025+)**

```
features/catalog/
├── model/                  ← доменные модели, интерфейсы, мапперы (чистый TS)
├── data/                   ← API-клиенты + репозитории (в
├── state/                  ← SignalStore / сигналы (долгоживущий стейт)
├── services/               ← use-cases, чистая бизнес-логика (без состояния)
├── facade/                 ← catalog.facade.ts — публичный API фичи
├── pages/                  ← *.page.ts + catalog.routes.ts
├── ui/                     ← dumb-компоненты, чистый UI
├── catalog.routes.ts       ← lazy-loadable роуты (если не в pages/)
└── index.ts                ← публичный контракт фичи
```

**3. Потоки данных — главный канон (никогда не нарушать)**

```
UI (pages/ui) 
  → Facade 
    → Services (use-cases) 
      → Store (patchState / методы) 
      → Data (repositories) 
        → API

Обратный поток только через сигналы:
Data → Store → Facade → UI
```

UI общается ТОЛЬКО с Facade.  
Facade — единственная точка входа в фичу.

**4. Уровни состояния (три, не больше)**

1. UI-state → локальные сигналы в компоненте (формы, табы, модалки)  
2. Feature-state → SignalStore внутри фичи (каталог, корзина, заказ)  
3. Global-state → только auth, theme, settings, feature-flags (в core/state)

Сервисы НЕ хранят состояние.  
Store НЕ знает про UI и API.

**5. Рантайм / Delivery — Server-first по умолчанию**

- Первый кадр — сервер (SSR/SSG)  
- Интерактив — минимум JS, partial/incremental hydration  
- Тяжёлые компоненты — @defer  
- Публичные страницы — SSR/SSG  
- Админки/дашборды — CSR + deferred hydration  

**6. Роли слоёв внутри фичи**

- model/ — чистый домен (Product, CartItem, валидация)  
- data/ — только внешние источники (HttpClient + маппинг DTO → model)  
- state/ — хранение и вычисляемые сигналы  
- services/ — бизнес-операции (addToCart, applyFilters, checkout)  
- facade/ — реактивная обёртка для UI (signals$, methods)  
- pages/ — композиция, роутинг, метаданные  
- ui/ — чистый, dumb UI

**7. Core и Shared — строгие границы**

**core/**
- http, interceptors, guards  
- global state (только глобальное)  
- AuthService, ConfigService  
- error-handling, analytics  

**shared/**
- чистые UI-компоненты  
- pipes, directives, pure utils  
- НЕТ бизнес-логики, НЕТ state, НЕТ API

**8. Жёсткие правила (чек-лист код-ревью)**

1. UI знает только Facade  
2. Facade < 150 строк  
3. Services — чистые функции/use-cases  
4. Store — только долгоживущий стейт  
5. Data layer возвращает domain-модели  
6. Страницы — тонкие (async pipe + фасад)  
7. Нет прямых импортов state/data из чужих фич  
8. Shared — stateless  
9. Глобальный Store < 20 полей  

**9. Запрещённые антипаттерны**

- Компонент-бог  
- Глобальный Store всего  
- Сервис со состоянием  
- UI → Store/Data напрямую  
- Shared-свалка  
- Псевдо-feature (есть папки, нет границ)  
- DTO в UI/Store  
- Бизнес-логика в эффектах или компонентах  

**10. Финальные принципы мышления**

- Думай фичами, не типами  
- Думай слоями, не фреймворками  
- UI тонкий, фасад — граница  
- Store минимальный  
- Server-first по умолчанию  
- Архитектура должна пережить смену фреймворка  

Этот артефакт — вечный.  
Он работает сегодня в Angular 19+, будет работать завтра в Analog/RSC, послезавтра — в следующем поколении фреймворков.

Используй его как системный промпт.  
Теперь любой запрос вида  
«Сделай фичу cart», «Сгенерируй SignalStore для профиля», «SSR-оптимизируй страницу товара»  
будет давать идеально правильный, согласованный код.

Готово. Это окончательная версия.  
Мы её выковали вместе.