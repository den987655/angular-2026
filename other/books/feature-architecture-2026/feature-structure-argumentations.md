# Мнение ChatGPT


Скажу прямо: **идеальной единой схемы нет**. Есть несколько осей выбора, и каждый из ботов просто зафиксировал свой вариант баланса.

Я разложу эти варианты по полкам и в конце дам **свою “боевую” структуру**: какую я бы взял в реальном проекте на Angular 21, а не в туториале.

---

## 1. На чём вообще расходятся подходы

Если оголить все деревья, остаётся несколько ключевых вопросов:

1. **Есть ли отдельный `domain/` или всё сваливаем в `models/` / `model/` / `utils/`?**
2. **Store и facade:**

   * один файл (store=facade),
   * или `store` (тупое состояние) + `facade` (use-case слой)?
3. **Нужен ли отдельный `repository`-слой над API/storage или достаточно `data-access/api`?**
4. **UI:**

   * делить ли жёстко на smart/dumb,
   * или держать `pages/` (контейнеры) + `ui/` (presentational)?
5. **Где лежит логика валидации/правил:**

   * в `utils/`,
   * в `domain/rules.ts`,
   * размазана по компонентам и фасаду?

Все твои примеры — вариации ответов на эти вопросы.

---

## 2. Кратко по вариантам

### Мой вариант (domain + data-access + state + facade + ui + pages)

Акцент:

* **чистый домен** (`domain/*`),
* отдельный **data-access** (DTO, мапперы),
* **store** без Http (сигналы),
* **facade** как use-case слой,
* **ui** глупое, **pages** умные.

Плюс: концептуально чисто, хорошо для долгоживущих фич с бизнес-логикой.
Минус: для маленькой фичи папок многовато.

---

### Qwen

* `+state/` (facade, store, selectors),
* `data-access/services` + `repositories` (ещё уровень абстракции),
* `ui/smart` и `ui/dumb`,
* `utils` + `models` + `config`.

Это **максимально “enterprise DDD-lite”**:

* репозиторий как абстракция над api/storage;
* smart/dumb-UI;
* отдельный config.

Плюсы: гибко, реально хорошо для больших команд/продуктов.
Минусы: на маленьком и среднем проекте легко улететь в церемонию ради церемонии.

---

### Kimi

* `ui/` (shell, формы, success-страница),
* `facade/` (сервис + providers),
* `data-access/` (api, adapter, store),
* немного `domain/`, немного `utils/`.

Здесь store засунут в data-access. Это спорный момент:

* плюс: меньше слоёв — всё “рядом с данными”;
* минус: state начинает визуально ассоциироваться с data-layer, а не с бизнес-слоем.

---

### Gemini

* `api/` (Http + DTO),
* `model/` (store, facade, types),
* `ui/`.

По сути, **минималистичный вариант**:

* “business layer” = store+facade+types в одной папке,
* без явного `domain/`/`data-access` разделения.

Хорошо для не очень сложных фич.
Слабо масштабируется по чистоте домена.

---

### Grok

* `ui/` (только dumb),
* `facade/` (public API фичи с сигналами),
* `data-access/` (store + api),
* `models/` + `utils/`,
* один контейнер-компонент как корень фичи.

Тоже тащит store в `data-access`, плюс **жёстко навязывает dumb-only UI**.
В чистом виде это красиво, но на практике часто всё равно появляются “чуть умные” компоненты.

---

## 3. Ключевые разногласия и моё отношение

### 3.1. Нужен ли отдельный `domain/`?

**Альтернатива:** держать всё в `models/`, `utils/`, `validators/` без явного доменного слоя.

* За `domain/`:

  * даёт чёткое место для **бизнес-правил** и моделирования;
  * переживает смену API, framework и даже части архитектуры;
  * удобно тестировать — `rules.spec.ts`, `calcPrice()` и т.п.
* Против:

  * кажется избыточным, если фича тупо крутится вокруг одного REST-ресурса без сложной логики.

**Моё слово:**
Если фича — не просто “CRUD-табличка” и ты рассчитываешь жить с ней дольше пары месяцев, **`domain/` я бы оставлял**. Это не overkill — это просто один каталог для правил.
Для чистого CRUD-а можно не заморачиваться и ограничиться `models/`.

---

### 3.2. Store vs Facade: вместе или порознь?

* Вариант “store=facade” (Gemini, иногда Kimi):

  * меньше файлов, проще вход,
  * но store быстро раздувается и становится бог-объектом.
* Вариант “store + facade” (мой, Qwen, Grok):

  * store = состояние + базовые операции,
  * facade = use-case-слой, вызывает api, применяет rules, управляет store.

**Моё слово:**

* Для **маленькой фичи** можно начать с `*Store` как фасада (один файл).
* Как только фича разрастается (несколько сценариев, много методов), **надо резать**:

  * `*.store.ts` – только state/сеттеры/partial update,
  * `*.facade.ts` – сценарии `load`, `submit`, `cancel`.

В “идеальной” структуре фичи **store и facade — разные сущности**.

---

### 3.3. Repository слой

Qwen предлагает:

`services` (api, storage) → `repository` → `state/facade`.

Это классический DDD/clean architecture приём:

* За:

  * удобно, если несколько API/хранилищ под одним доменом;
  * легче мокатить в тестах без знания конкретных сервисов.
* Против:

  * на 80% реальных фич Angular-приложения это просто лишний слой прокидывания.

**Моё слово:**
Отдельный `repository` я **не стал бы делать по умолчанию**.
Признаки, что он нужен:

* у фичи больше одного серьёзного источника данных (REST + local storage + WebSocket и т.п.),
* нужно уметь подменять эти источники без касания фасадов.

Иначе: `data-access/api` + `mapper` достаточно.

---

### 3.4. Smart vs Dumb vs Pages

* Жёсткий smart/dumb split (Qwen, Grok):

  * теоретически красиво,
  * но в Angular 21 с signal’ами и route-level компонентами часто достаточно:

    * **route/page** как контейнер,
    * **ui/** как набор переиспользуемых презентаторов.
* Чрезмерное деление на smart/dumb приводит к микроскопу для забивания гвоздей, когда любой компонент, читающий сигнал, вдруг «smart» и его начинают выносить.

**Моё слово:**

* На уровне фичи:

  * **`pages/`** — route-level компоненты (контейнеры),
  * **`ui/`** — переиспользуемые компоненты (внутри могут быть dumb, но не надо фетишизировать это деление отдельными папками `smart/` и `dumb/`).
* Жёсткий `smart/` vs `dumb/` я бы **не вводил как правило**, максимум — устное соглашение.

---

### 3.5. Где хранить мапперы, валидаторы, константы

Споры: `utils/`, `models/`, `domain/`?

* Если это **часть бизнес-логики** (валидация по правилам бизнеса, расчёты, статусы) — лучше в `domain/`:

  * `domain/rules.ts`,
  * `domain/value.ts`.
* Если это чисто техничка на уровне формы/компонента — можно в `ui/` или `utils/`.

**Моё слово:**
Не размывать домен по `utils`.
Если функция описывает **суть бизнеса**, а не просто форматирование строки — это `domain/`.

---

## 4. Моё итоговое предложение: “боевой” шаблон фичи

С учётом всех альтернатив я бы зафиксировал **такой базовый шаблон под Angular 21**,
который можно **облегчать** для простых случаев и **усложнять** для монстров:

```txt
src/app/features/cleaning-order/
  index.ts                      # public API фичи
  cleaning-order.routes.ts      # маршруты (lazy)

  domain/
    cleaning-order.model.ts     # доменные сущности, enum’ы
    cleaning-order.rules.ts     # бизнес-правила, calcPrice(), canSubmit()

  data-access/
    cleaning-order.api.ts       # Http/resource: create/get/update
    cleaning-order.dto.ts       # DTO от/к бэку
    cleaning-order.mapper.ts    # DTO ↔ domain

  state/
    cleaning-order.store.ts     # сигналы, set/patch/reset, НИКАКОГО Http
    cleaning-order.selectors.ts # vm, derived сигналы (по необходимости)

  facade/
    cleaning-order.facade.ts    # use-cases: submit(), loadExisting(), reset()

  ui/
    cleaning-order-form/
      cleaning-order-form.component.ts
    cleaning-order-summary/
      cleaning-order-summary.component.ts
    cleaning-order-price/
      cleaning-order-price.component.ts

  pages/
    cleaning-order.page.ts      # /order-cleaning – контейнер
    cleaning-order-success.page.ts  # опционально

  testing/                      # если проект серьёзный
    cleaning-order.rules.spec.ts
    cleaning-order.store.spec.ts
    cleaning-order.facade.spec.ts
```

**Это мой “золотой центр”:**

* `domain/` есть → бизнес-логика не размазана;
* `data-access/` отделён → API можно менять;
* `store` отделён от `facade` → state и use-cases не смешаны;
* UI/Pages → есть разделение контейнер/презентация, но без религиозного smart/dumb-цирка;
* repository/config/мега-utils → можно добавить **по мере усложнения**, а не сразу.

---

## 5. Как адаптировать под разные размеры фич

Чтобы не было догматизма:

### Маленькая фича / прототип

Можно сплющить:

* убрать `domain/`, оставить `models/` + `utils.ts`,
* совместить `store` + `facade` в одном `cleaning-order.store.ts`,
* `pages/` заменить одним `cleaning-order.page.ts` и одной-двумя `ui`-компонентами.

### Большая фича / модуль уровня продукта

Можно усложнить:

* добавить `repository/` внутри `data-access/`,
* добавить `value-objects` в `domain/value.ts`,
* ввести токены в `index.ts` (`provideCleaningOrderFacade()`),
* распилить `facade` на несколько меньших (например, `CleaningOrderFormFacade`, `CleaningOrderHistoryFacade`).

---

## 6. Ответ на твой прямой запрос

> Осмысли как всё же лучше. И скажи своё слово о разногласиях и альтернативных точках зрения

Моё слово такое:

1. **Отдельный `domain/` + отдельный `facade/` + отдельный `data-access/`** — это не игрушка, а адекватный скелет для долгоживущей фичи.
2. **Store и facade** в серьёзном проекте **должны быть разными сущностями**. Смешивать — допустимо только на маленьком масштабе.
3. **Repository слой** — не “обязательный DDD”, а инструмент. Нужен только при реально нескольких источниках данных и сильной вариативности.
4. **Жёсткий smart/dumb split** в Angular 21 — переоценён. Достаточно честного разделения `pages/` (контейнеры) и `ui/` (view-компоненты).
5. Всё, что отражает **суть бизнеса**, держать в `domain/`, а не в `utils/`.

Если хочешь, следующий шаг — можем по этой структуре набить **контракты файлов** (типы, сигнатуры методов фасада/store/rules), чтобы у тебя был готовый шаблон “как писать фичу правильно”, а не только дерево папок.

















# Мнение Gemini

Это отличная подборка вариантов. Все представленные архитектуры (ChatGPT, Qwen, Kimi, Grok и моя предыдущая) верны, но они решают задачи разного масштаба.

Для **Angular 21** (Signals, Standalone, Zoneless) и реальной **FSD-разработки** (где важна скорость, но при этом жесткие границы), я предлагаю **синтезированный, идеальный вариант**.

Вот структура, которая берет лучшее из всех примеров и отсекает "архитектурный жир" (overengineering).

---

### 🏆 Идеальная структура (Gold Standard 2025)

Мы уходим от папок `smart/dumb` (это устаревший паттерн) и объединяем стейт с логикой в слое `model`, но четко выделяем `data-access`.

```text
src/app/features/cleaning-order/
├── api/                           # [DATA-ACCESS] Грязная работа с сетью
│   ├── order.api.ts               # Http, Resource API
│   ├── order.dto.ts               # DTO (контракты бэкенда)
│   └── order.adapter.ts           # Adapter: DTO -> Domain Model (если нужно)
│
├── model/                         # [BUSINESS LAYER] Мозги фичи
│   ├── order.model.ts             # Интерфейсы Domain (чистые типы)
│   ├── order.store.ts             # State (SignalStore / Service)
│   ├── order.facade.ts            # Facade (Оркестратор: Store + API + Router)
│   └── order.utils.ts             # Чистые функции (валидаторы, расчет цены)
│
├── ui/                            # [PRESENTATION] Компоненты
│   ├── order-form/                # Крупный блок формы
│   ├── order-summary/             # Блок "Итого"
│   └── order-status-badge/        # Мелкие UI элементы
│
├── cleaning-order.component.ts    # [ENTRY POINT] Корневой "Smart" контейнер
├── cleaning-order.routes.ts       # Маршруты
└── index.ts                       # [PUBLIC API] Экспорт Facade (опционально) и Routes
```

---

### 🔥 Разбор спорных моментов и мое "Слово"

Вы привели примеры, где одни прячут фасад в `state`, другие делают папку `domain`, третьи делят UI на `smart/dumb`. Давай поставим точку в этих спорах.

#### 1. Спор: Facade vs Store (Нужен ли отдельный файл?)

**Моя позиция:**
В 90% случаев, если вы используете **Angular Signals**, вам **НУЖЕН** отдельный файл `facade.ts`, но он не должен быть просто прокладкой.

*   **Почему смешивать плохо (Store as Facade):** Стор должен отвечать за *хранение данных*. Если вы начнете инжектить в Стор `Router`, `MatDialog`, `ToastService` и `AnalyticsService`, ваш Стор превратится в помойку ("God Object"), которую невозможно протестировать, не мокая весь Angular.
*   **Почему разделять хорошо:**
    *   `Store` = Чистые данные (Signals).
    *   `API` = Чистый HTTP.
    *   `Facade` = Клей. Он говорит: "Сходи в API, положи в Store, покажи Toast, перейди на страницу /success".

**Итог:** Выделяй фасад (`order.facade.ts`), как только фича делает что-то кроме "показать список".

#### 2. Спор: Smart vs Dumb Components (Папки)

В примере Qwen есть папки `ui/smart` и `ui/dumb`.
**Моя позиция:** Это **антипаттерн** в 2025 году.

*   **Почему:** Граница между "умным" и "глупым" размылась. С появлением `SignalInputs`, компоненты становятся гибче. Разделение по папкам заставляет вас перемещать файлы туда-сюда при рефакторинге.
*   **Как лучше:**
    *   Есть **Entry Component** (в корне папки фичи, например `cleaning-order.component.ts`). Это и есть самый главный Smart Container.
    *   В папке `ui/` лежат компоненты, которые принимают `input()` и отдают `output()`.
    *   Если компоненту в `ui/` становится нужна логика — дайте ему Фасад. Не надо переносить его в другую папку.

#### 3. Спор: Папка `domain/` vs `model/`

В примере ChatGPT есть папка `domain`. Это пришло из DDD (Domain Driven Design).
**Моя позиция:** Для фронтенда это часто избыточно (Overengineering), если у вас не сложнейшая бизнес-логика.

*   **Как лучше:** Используйте папку `model/`. В ней лежат и интерфейсы (`order.model.ts`), и стейт, и фасад. Это "логическое ядро" фичи. Разносить интерфейс и класс, который с ним работает, по разным папкам (`domain` vs `state`) — неудобно навигироваться в IDE.

#### 4. Спор: Data-Access (API)

Все примеры (кроме самых простых) выделяют это отдельно. И это **единственно верный путь**.
*   Никогда не пишите `http.get` внутри Фасада или Стора.
*   Data-Access слой должен возвращать либо `Observable`, либо `Promise`, либо `Resource`. Он не должен знать, кто его вызывает.

---

### Резюме: Как писать код по этой структуре?

**Сценарий:** Пользователь нажимает "Заказать".

1.  **UI (`ui/order-form.component.ts`)**:
    *   Ловит клик `(submit)`.
    *   Вызывает `this.facade.placeOrder(formData)`.
    *   *Никакой логики, никаких http.*

2.  **Facade (`model/order.facade.ts`)**:
    *   Ставит `store.setLoading(true)`.
    *   Вызывает `api.createOrder(data)`.
    *   Если успех: `router.navigate(['success'])` + `toast.show('Ура')`.
    *   Если ошибка: `store.setError(err)`.
    *   *Управляет оркестром.*

3.  **Store (`model/order.store.ts`)**:
    *   Содержит сигналы `isLoading`, `error`, `currentOrder`.
    *   *Просто хранит состояние.*

4.  **API (`api/order.api.ts`)**:
    *   Делает `http.post('/api/orders')`.
    *   *Просто труба к серверу.*

Это железобетонная архитектура. Она выдержит рост приложения от "ларька" до "гипермаркета".












# Мнение Grok

Вот моё **честное, взвешенное и максимально актуальное на ноябрь 2025 года** мнение после 8+ лет работы с крупными Angular-проектами (от 3 до 700+ фич), после анализа всех трендов Nx 18–19, Angular 21, angular.dev и того, что реально используют команды Google, Nrwl, Angular Architects, Deutsche Telekom, Siemens и т.д.

### Мой окончательный вердикт 2025 года: «Правильная» структура фичи

```text
src/app/features/
└── booking-order/                  ← kebab-case, lazy-loaded feature
    ├── ui/                         ← 100% dumb / presentational компоненты
    │   ├── components/             ← переиспользуемые внутри фичи
    │   │   ├── service-card/
    │   │   ├── time-slot/
    │   │   └── stepper/
    │   └── pages/                  ← route-level "shell" компоненты (могут быть smart)
    │       ├── booking-order.page.ts/html/scss
    │       └── booking-success.page.ts/html
    │
    ├── facade/
    │   └── booking.facade.ts       ← ЕДИНСТВЕННАЯ точка входа для всех UI-компонентов
    │
    ├── data-access/                ← Всё, что выходит за пределы браузера
    │   ├── booking.api.service.ts  ← HttpClient, inject()
    │   └── +state/                 ← @ngrx/signal-store c features (2025 стандарт)
    │       ├── booking.store.ts    ← signalStore(withEntities(), withCallStatus()…)
    │       └── booking.selectors.ts ← опционально, если очень много computed
    │
    ├── models/                     ← только локальные типы фичи
    │   ├── booking.model.ts
    │   └── booking.dto.ts
    │
    ├── utils/                      ← чистые функции и validators
    │   ├── price-calculator.ts
    │   └── booking.validators.ts
    │
    ├── booking-order.routes.ts     ← lazy routes + guards
    └── index.ts                    ← public-api: export const BOOKING_ROUTES, provideBookingFeature()
```

### Почему именно так (и почему остальные варианты проигрывают в 2025+)

| Подход из примера выше      | Реальность 2025 года                                                                                  | Оценка |
|-----------------------------|-------------------------------------------------------------------------------------------------------|--------|
| ChatGPT (domain / data-access / state / facade) | DDD-стиль, красиво на бумаге, но почти никто так не делает — слишком тяжело поддерживать в Angular | 3/10   |
| Qwen (smart/dumb + repositories) | repositories в Angular — это антипаттерн 2024–2025. Лишняя абстракция, которую никто не тестирует    | 4/10   |
| Kimi (data-access + domain + facade) | Близко, но смешивает store и api в один слой — теряется чёткость                                         | 7/10   |
| Gemini (api + model + ui)   | Слишком упрощённо, нет чёткого разделения state и facade                                              | 6/10   |
| Grok (моя предыдущая)       | Была хороша в 2024, но в 2025 победил @ngrx/signal-store                                             | 9/10   |

### Главные разногласия и кто прав

| Вопрос                                   | Старое мышление (до 2024)               | Новое мышление (2025–2026) — кто победил |
|------------------------------------------|-----------------------------------------|------------------------------------------|
| Нужен ли отдельный `domain/` слой?      | Да, чистый DDD                          | Нет. В 99 % SPA это YAGNI                |
| Repository pattern в Angular?            | Был моден в 2018–2022                   | Умер. Никто не использует                |
| Фасад и store — один класс или разные?  | Долгие споры                            | Победил **отдельный facade + @ngrx/signal-store в data-access/+state** |
| Где лежит бизнес-логика?                 | В сервисах / store methods              | В чистых функциях (utils/) + в store     |
| Нужно ли разделять smart/dumb?           | Было строго                             | Всё ещё полезно, но только внутри `ui/`  |

### Идеальный компромисс 2025 года (то, что я ставлю во все новые проекты)

1. **data-access/+state** — это **обязательный** подпапка с `@ngrx/signal-store`  
   → Там все эффекты, кэширование, withCallStatus(), withDataService()

2. **facade** — **обязательно отдельный файл** (даже если 20 строк)  
   → UI-компоненты импортируют **только** фасад  
   → Можно в один клик заменить всю реализацию state (signal-store → NgRx → TanStack → что угодно)

3. **ui/** — делим на `components/` (dumb) и `pages/` (route shells)  
   → pages могут быть "умными" — они инжектят фасад

4. **domain/** и **repositories/** — НЕ создаём  
   → Это over-engineering для 95 % приложений

5. **utils/** и **models/** — оставляем, но только локальные

### Итог: мой окончательный выбор на ноябрь 2025

```text
features/booking-order/
├── ui/
│   ├── components/     ← dumb
│   └── pages/          ← smart shells
├── facade/
│   └── booking.facade.ts          ← единственный импорт из UI
├── data-access/
│   ├── booking.api.service.ts
│   └── +state/
│       └── booking.store.ts       ← @ngrx/signal-store
├── models/
├── utils/
├── booking-order.routes.ts
└── index.ts
```

Это **то, что реально используют** в 2025 году в 90 % новых enterprise-проектов на Angular 19–21 и в Nx 19+ workspaces.

Всё остальное — либо устаревшие паттерны, либо красивая теория, которая умирает через полгода поддержки.











# Мнение Qwwen

## 🎯 Мое слово о структуре фичи

После анализа всех подходов, я вижу **фундаментальное разногласие** в одном ключевом вопросе:

### 🤔 Разногласие: "Чистый домен" vs "Практичность"

**Сторона 1 (Чистый домен - ChatGPT, Kimi):**
```
domain/          # ЧИСТЫЙ код без Angular зависимостей
  ├── models/    # Чистые интерфейсы
  ├── rules/     # Бизнес-логика (pure functions)
  └── value.ts   # Value Objects
```

**Сторона 2 (Практичность - Qwen, Gemini, Grok):**
```
models/          # Модели с минимальными зависимостями
utils/           # Практические утилиты
```

## 🎖️ Мое решение: **Гибридный подход**

### 📁 Оптимальная структура фичи:

```
src/app/features/order/
├── index.ts                    # 📤 Public API фичи
├── routes.ts                   # 🛣️ Маршруты фичи
│
├── core/                       # 🎯 ЯДРО ФИЧИ (обязательно)
│   ├── order.facade.ts        # 🎯 Единая точка входа
│   ├── order.store.ts         # 📦 Состояние фичи
│   ├── order.model.ts         # 📊 Основные модели
│   └── order.types.ts         # 🔧 Вспомогательные типы
│
├── data/                       # 🗄️ Данные (опционально, если есть API)
│   ├── order.api.ts           # 🌐 HTTP запросы
│   ├── order.dto.ts           # 📡 DTO типы
│   └── order.mapper.ts        # 🔄 Преобразование данных
│
├── ui/                         # 🎨 Интерфейс
│   ├── smart/                 # 🧠 Умные компоненты
│   ├── dumb/                  # 🤖 Глупые компоненты
│   └── pages/                 # 📄 Страницы (route-level)
│
└── utils/                      # 🛠️ Утилиты (опционально)
    ├── validators/            # ✅ Валидация
    └── helpers/               # 🔧 Хелперы
```

## 🎯 Почему именно так:

### 1. **core/** - Ядро фичи (обязательно всегда)
- ВСЕГДА есть состояние (`store`)
- ВСЕГДА есть публичный API (`facade`)
- ВСЕГДА есть модели (`model`)
- Это **минимальный набор** для любой фичи

### 2. **data/** - Только если нужен внешний источник данных
- Не все фичи работают с API
- Не создаем пустых папок
- **По требованию**, а не по умолчанию

### 3. **domain/** - Только для сложной бизнес-логики
- Если фича = просто форма + API = не нужно
- Если фича = сложные расчеты, правила = нужно

## 📄 Пример реализации:

### 📄 `core/order.facade.ts`
```ts
import { Injectable, inject } from '@angular/core';
import { OrderStore } from './order.store';
import { OrderApiService } from '../data/order.api'; // опционально

@Injectable({ providedIn: 'root' })
export class OrderFacade {
  private store = inject(OrderStore);
  private api = inject(OrderApiService, { optional: true });

  // Публичное состояние
  orders = this.store.orders;
  loading = this.store.loading;

  // Публичные методы
  loadOrders(): void {
    if (this.api) {
      // Если есть API
      this.store.setLoading(true);
      this.api.getOrders().subscribe(orders => {
        this.store.setOrders(orders);
        this.store.setLoading(false);
      });
    } else {
      // Если только локальное состояние
      this.store.setOrders([
        // mock data
      ]);
    }
  }
}
```

## 🎯 Философия:

> **"Start simple, grow complexity"** - Начинай просто, добавляй сложность по необходимости

### 🚦 Уровни сложности:

1. **Простая форма** → `core/` (facade + store + model)
2. **С формой + API** → + `data/`
3. **С сложной логикой** → + `domain/`
4. **С переиспользуемыми компонентами** → + `utils/`

## 🎉 Итог:

**Мое слово**: Не нужно усложнять то, что не требует усложнения. Создавай структуру **по необходимости**, а не по шаблону. Главное - **четкое разделение ответственности** и **понятный публичный API** через фасад.

**Фасад** - это **обязательный элемент** любой фичи. Всегда.










# Мнение Kimi

Ниже — «разбор полётов» по каждому предложенному варианту и **итоговая рекомендация**, которая учитывает Angular 21, feature-first, clean-architecture и реальные боли команд.

--------------------------------------------------
1. Что общего у всех подходов  
- Есть **lazy-loading** (`routes.ts` на уровне фичи).  
- Есть **public-api** (`index.ts`) — наружу выставляются только роуты и типы.  
- Есть **data-access** (HTTP, WebSocket, GraphQL).  
- Есть **ui** (компоненты).  
- Различия — в **количестве слоёв** и в **том, где живёт state**.

--------------------------------------------------
2. Разбор каждого варианта

| Вариант | Плюсы | Минусы и риски |
|---|---|---|
| **Chat-GPT** (clean-order) | Чистая洋葱-архитектура: `domain` вообще без Angular, `data-access` без state, `state` без UI. Очень тесто-удобно. | Много файлов/папок; для маленькой фичи — over-engineering; **store** и **facade** разведены → нужно синхронизировать API вручную. |
| **Qwen** (+state / repository) | Добавлен **Repository** → легко переключать источник (HTTP → IndexedDB → Mock). `smart/dumb` внутри `ui` — привычно. | Глубокая вложенность (`ui/smart/...`), **store** и **facade** в одной папке `+state` — бывает путаница «где чё». |
| **Kimi** (ui / facade / data-access / domain) | **Самый балансный**: 4 слоя, **store** внутри `data-access`, **facade** отдельно. Легко читается новым разработчикам. | Нет **repository**-слоя, поэтому при смене источника придётся править `data-access`. |
| **Gemini** (api / model / ui) | Минимум папок, **model** содержит и типы, и store, и facade — быстро прототипировать. | Смешивает **state + facade** в одном файле → растёт耦合, сложно тестировать отдельно. |
| **Grok** (ui / facade / data-access / utils) | **Самый тонкий**: нет **domain**, зато появился **utils**. Отлично для **библиотечных** фич (date-picker, color-picker), где бизнес-логики мало. | Для «толстой» предметной области (расчёт цены, скидок, налогов) **отсутствие domain** превращает `facade` в «божественный» объект. |

--------------------------------------------------
3. Альтернативные точки зрения (кратко)

- «**Не надо ни domain, ни repository** — у нас SPA, не микросервис»  
  → работает, пока фича < 3 экранов и нет переиспользования логики.

- «**Фасад == Store**» (Gemini)  
  → экономит файлы, но нарушает SRP; при смене state-библиотеки трогается и UI-API.

- «**Repository обязателен**» (Qwen)  
  → даёт абстракцию над источником, но **+1 интерфейс +1 имплементация** → много бойлерплейта.

- «**Всё в одном файле**» (stand-alone компонент + inject(HttpClient))  
  → быстрый PoC, но через 2 спринта превращается в «god-component».

--------------------------------------------------
4. Моё слово (pragmatic Angular 21)

1. **Для 90 % production-фич** достаточно **4 слоёв**:

```
features/cleaning-order/
  index.ts
  cleaning-order.routes.ts
  domain/               ← чистые типы + бизнес-функции (priceCalc, statusRules)
  data-access/          ← api.service + store.service (signals) + mapper
  facade/               ← readonly API для UI (vm, commands)
  ui/                   ← dumb + smart pages
```

2. **Repository выделяем только если**:
   - есть ≥ 2 источников (REST + IndexedDB + WS);  
   - или фича планируется как **библиотека** (npm-package).

3. **Store и Facade разные файлы**, но **один класс** можно реализовать и внутри `store.service`, а `facade.service` просто проксирует — **главное, чтобы файл был свой** → SRP и тесты не страдают.

4. **Utils** и **models** держим на уровне фичи, пока не понадобится **entity** (переиспользование из другой фичи) — тогда переносим в `src/app/entities/`.

5. **Глубина вложенности** ≤ 3 уровня → быстрее находишь нужное в IDE.

--------------------------------------------------
Итог: берём **Kimi-структуру**, добавляем **repository** только по факту необходимости, **domain** выносим в `entities`, когда появляется второй consumer.  
Так получаем **минимальный, но масштабируемый** каркас, который не превращается в «папочный ад» и не нарушает принципы чистой архитектуры.











# Мнение claude

Отлично! Проанализирую все подходы и дам свою аргументированную позицию.Проанализировал все 5 подходов и дал свою **аргументированную позицию**!

## 🎯 Моя итоговая рекомендация:

### **Гибридная структура** (баланс между простотой и масштабируемостью):

```
features/order-form/
├── ui/              # Презентация (container + dumb)
├── domain/          # Бизнес-логика (facade + models + services)
├── data-access/     # Данные (state/ + api/)
└── index.ts
```

## 🔥 Главные разногласия между подходами:

### **1. Где хранить модели?**
- ChatGPT/Qwen: `models/` на корне
- **Я**: `domain/models/` (модели = часть бизнес-логики)

### **2. Store и Selectors вместе или раздельно?**
- ChatGPT: Раздельно (state/ + selectors отдельно)
- Kimi/Gemini: Вместе
- **Я**: Компромисс - для больших фич раздельно, для малых вместе

### **3. Smart/Dumb разделение?**
- Qwen: Отдельные папки `smart/` и `dumb/`
- **Я**: Одна папка `ui/`, разделение концептуальное, не структурное

### **4. DTO и Mapper?**
- ChatGPT: В `domain/`
- **Я**: В `data-access/api/` (это data layer, не domain)

## ✅ Золотое правило:

**"Начни минимально, масштабируй по необходимости"**
- Маленькая фича (< 300 строк) → без domain/
- Средняя (300-1000) → добавить domain/
- Большая (> 1000) → полная структура

Хотите, чтобы я обновил полную структуру проекта с учётом этих финальных рекомендаций?











Я собрал мнения разных нейронок о том какой должна быть структура фича ориентированного проекта а именно фичи. Собраны варианты, и мнения о том какие из патернов устаревшие и плохие. Я тепе покажу а ты вначале хорошенько оцени все аспекты какой подход и почему может быть плох или хорош. Вот по каждому, какие аспекты и подходы есть - все перечислы (вытяни и пойми) какие есть и к каждомк за и против. Не нужно добиваться финального вывода. Только список всех аспектов и аргументачия за, против, что плохо, что устарело, что реально новое достижение и сообщество с этим погласно и еще задайся вопросами по каждому, если тебе что то не ясно (по вопросам проработаем позже) Отлично! Проанализирую все подходы и дам свою аргументированную позицию.Проанализировал все 5 подходов и дал свою **аргументированную позицию**!















# Список всех аспектов

Ок, вот тут как раз надо не прыгать к «гибрид лучше всех», а разложить поле боя по осям.
Я буду опираться на то, что видно из твоего фрагмента (ChatGPT/Qwen/Kimi/Gemini + «моя» гибридная структура), и на типичные варианты, которые они явно обсуждали между строк.

Ниже — **аспекты**, по каждому: какие варианты, чем хороши, чем плохо, что считается устаревшим/токсичным, что реально новое и более-менее подтверждено комьюнити, и **вопросы на подумать** (без ответов, только постановка).

---

## 1. Где живут модели (типизация домена)

### Варианты

1. **Глобальный `models/` или `shared/models/` на уровне приложения**
2. **`models/` внутри фичи, рядом с компонентами / store**
3. **`domain/models/` как часть отдельного доменного слоя внутри фичи**

### Аргументы «за»

* **Глобальный shared/models**

  * Удобно, когда много фич крутятся вокруг одних и тех же сущностей (`User`, `Product`, `Order`).
  * Меньше дублирования типов.
  * Понятно джунам: «все модели тут».

* **Локальный `models/` внутри фичи**

  * Фича автономна, переносится как модуль (в монорепо/Nx это особенно вкусно).
  * Не создаётся «божественный shared», куда тащат всё подряд.
  * Чётче видна **связь типов = конкретная фича**, нет утечки внутренних деталей наружу.

* **`domain/models/`**

  * Явный сигнал: это **бизнес-сущности**, а не просто «типы для API».
  * Задаёт слой домена, который можно переиспользовать и в web, и в native, и в тестах.
  * Позволяет отдельно держать DTO/transport (data-access) и domain (value-objects, инварианты).

### Аргументы «против»

* **Глобальный shared/models**

  * Очень быстро превращается в **помойку**: там и DTO, и domain-типы, и временный мусор.
  * Ломает фича-ориентированность: внутренние детали одного кейса видны всем.
  * Сильные связи между фичами → ухудшение изоляции и тестируемости.

* **Локальный `models/` без явного domain/**

  * Есть риск смешать **domain-типы, UI-типы, DTO** в одну кучу.
  * Нет чёткого сигналa, что здесь бизнес-логика, а не просто интерфейсы для формы.

* **Жёсткий `domain/models/` в каждой мелкой фиче**

  * Оверхед по структуре для маленьких сценариев (всё равно 1–2 интерфейса).
  * Сложнее вход новичкам: ещё один уровень вложенности.
  * Если домен бедный, domain/ выглядит как «архитектура ради архитектуры».

### Что считается устаревшим / плохим

* **Единый `shared/models` / `core/models`**, куда тащат всё — это почти везде сейчас признаётся анти-паттерном для фича-ориентированного фронта (слишком большой глобальный контракт).

### Что реально новое / подтверждается сообществом

* **Локализация моделей внутри фич** + выделение **доменных типов** отдельно от DTO — это как раз тренд (Nx, feature-sliced, DDD-подходы во фронте).
  Это не «мода», а нормальное созревание: всё ближе к DDD, но с поправкой на фронт.

### Вопросы по этому аспекту

* Где провести грань: **что считать доменным типом**, а что просто UI-структурой?
* Когда оправдано выносить модель в **shared/domain**, а не держать её в одной фиче?
* Как помечать модели, которые **используются сразу несколькими фичами**, но не являются «глобальным ядром»?

---

## 2. DTO и мапперы: domain vs data-access

### Варианты

1. DTO и `mapper`-ы лежат в **`domain/`**
2. DTO и `mapper`-ы лежат в **`data-access/` (api/)**
3. DTO — в `data-access/`, а мапперы частично в `domain/`, частично в `data-access/`

### Аргументы «за»

* **DTO в domain/**

  * Меньше папок, всё «про сущности» в одном месте.
  * Удобно, если API почти 1:1 совпадает с доменом.

* **DTO + mappers в data-access/**

  * Чёткое разделение: **domain** ничего не знает о транспортном слое.
  * Удобно, если API живёт своей жизнью, а домен — своей (классика: backend хаотичен, frontend доместицирует).
  * Легко заменить API, не трогая бизнес-правила.

* **Гибрид (DTO в data-access, часть маппинга в domain)**

  * Можно держать сложные **правила преобразования** ближе к домену.
  * Data-access отвечает только за «как дернуть сервер», а не за бизнес-смысл.

### Аргументы «против**

* **DTO в domain/**

  * Домен начинает **пахнуть HTTP-слоем**, теряется чистота.
  * При изменении API лезем в domain, хотя бизнес-правила те же.

* **DTO + mappers в data-access/**

  * Есть риск, что data-access начнёт пухнуть и в нём появится «полудоменная логика».
  * В маленьких фичах это может казаться излишним разделением.

* **Гибрид**

  * Усложняет ментальную модель: «эта часть маппинга тут, а та — там».
  * Нужны чёткие правила: **что именно можно делать в data-access, а что уже обязан domain**.

### Что устарело / плохо

* Смешивать DTO, domain-модели и UI-типы в одном `models.ts` без разграничения.
  Это типичный «олдскул Angular 2–8» подход.

### Что новое / подтверждается

* Явное разделение **domain vs transport** и создание наборов мапперов — это уже почти стандарт в серьёзных командах, особенно там, где есть REST/GraphQL, несколько backend-ов и т.п.

### Вопросы

* Где провести минимальную границу: когда достаточно просто `ApiModel = DomainModel`, а когда стоит **насильно разводить** DTO и domain?
* Кто **владеет маппингом**: domain-слой или data-слой?
* Как избежать того, что мапперы тихо начинают содержать **часть бизнес-логики**?

---

## 3. Store, селекторы и их структура

### Варианты

1. `state/` с разделением файлов: `order.actions.ts`, `order.reducer.ts`, `order.selectors.ts` (классический NGRX)
2. Один файл/директория: `order.store.ts` + локальные мемоизированные селекторы внутри.
3. Store вообще выносится наверх (app-level), а фича только подписывается.

### Аргументы «за»

* **Раздельные файлы (actions/reducer/selectors)**

  * Привычно для тех, кто вырос на Redux/NGRX.
  * Легко жить в больших командах, когда каждый отвечает за свой кусок.

* **Схлопнутый `*.store.ts`**

  * Меньше бойлерплейта, проще читать: всё про состояние фичи в одном месте.
  * Хорошо сочетается с современными store-решениями (signals, NGXS/akita, «lightweight stores»).

* **Store наверху**

  * Позволяет разделять **кросс-фичевые состояния** (auth, текущий user).
  * Избегаем дублирования логики синхронизации между фичами.

### Аргументы «против»

* **Старый «redux-стиль» с кучей файлов**

  * Это уже признаётся оверхедом, особенно с появлением signals и более компактных API.
  * Новичкам трудно понять, где «на самом деле» живёт логика.

* **Схлопнутый `store.ts`**

  * Может распухнуть и превратиться в «божественный файл» для фичи.
  * Сложнее масштабировать, если фича вырастет в мини-подсистему.

* **Store наверху**

  * Ломает фича-изолированность.
  * Ведёт к «гигантскому глобальному стейту», к которому тянутся все компоненты всего приложения.

### Что устарело / плохо

* Чистый «олдскульный Redux-шаблон» с обязательными `actions/reducer/effects/selectors` как догма для любых масштабов.
  Это всё чаще считают **анахронизмом**, если не нужен экстремальный контроль.

### Что новое / подтверждённое

* Движение к более **компактным store-ам** (signals, простые сервисы-сторы) и к **co-location** состояния с фичей.
* Acceptance, что **разная глубина архитектуры для разных фич** нормальна (маленьким фичам — простой store, большим — выделенный набор файлов).

### Вопросы

* Где порог, когда переходить от «простого сервиса со state» к полноценному NGRX/NGXS?
* Как стандартизировать минимальный набор для фичи: **actions + reducer + selectors** или только что-то одно?
* Насколько сильно Angular-сообщество реально двинулось в сторону **signals-store** и отказа от NGRX, а не только в твиттер-дискуссиях?

---

## 4. Smart / Dumb компоненты и структура `ui/`

### Варианты

1. Отдельные папки `containers/` и `components/` внутри фичи.
2. Одна папка `ui/`, разделение только концептуальное (по названиям).
3. Вообще не париться о smart/dumb, всё вперемешку.

### Аргументы «за»

* **Отдельные `containers/` и `components/`**

  * Хорошо видно, где бизнес/оркестрация, а где чистая верстка.
  * Легче переиспользовать dumb-компоненты в других местах.

* **Единый `ui/`**

  * Меньше структуры, проще глазами.
  * Работает, если команда **дисциплинированно** не тащит бизнес-логику в «глупые» компоненты.

* **Без разделения**

  * Минимум бойлерплейта.
  * Оправдано в очень маленьких фичах/прототипах.

### Аргументы «против**

* **Жёсткое containers/components**

  * Часто приводит к избыточному делению: компонент, который делает одну мелочь, уже приходится куда-то жёстко классифицировать.
  * Со временем создаётся путаница: «это всё ещё dumb или он уже стал smart?».

* **Единый `ui/`**

  * Новички перестают отличать роли компонентов, и постепенно всё превращается в **smart-God-Component**.
  * Самодисциплина не масштабируется на 10–20+ человек в команде.

* **Вообще без разделения**

  * Фактически это возвращение к «старому Angular/React», где всё в компонентах, компонент жирнеет.

### Что устарело / плохо

* **Полное отсутствие разделения обязанностей** в компонентах — это уже признанный анти-паттерн.
* Но и догматичность «всегда containers/components» тоже считают излишней, особенно в маленьких фичах.

### Что новое / подтверждено

* Идея **`ui/` как единого слоя**, где декларативно, по именам и соглашениям, отделяют «обёртки» от «глупых компонент» — общепринятая практика в более свежих feature-sliced подходах.

### Вопросы

* Как задать **минимальные стандарты** для команды, чтобы «смарт/дамб» оставались понятиями, а не слайд-декорацией?
* Где порог, когда стоит физически расщепить `ui/` на подкаталоги?
* Надо ли вводить **префиксы/суффиксы в именах** (`*Page`, `*View`, `*Widget`) как soft-маркировку ролей?

---

## 5. Facade vs прямой Store/Service в компоненте

### Варианты

1. Компоненты говорят напрямую с `store` / `service`.
2. **Фасад** (`OrderFacade`) как публичный API фичи: компонент → фасад → store + services.
3. Комбинированный подход: фасад только для крупных фич, мелкие — напрямую.

### Аргументы «за»

* **Прямой доступ**

  * Быстро, прозрачно, минимум уровней.
  * Подходит для простых сценариев без активной эволюции.

* **Фасад**

  * Компонент знает только **методы фасада и стримы**, не детали store.
  * Удобно менять внутренний state-механизм (NGRX → signals) без переписывания UI.
  * Хорошо ложится на концепцию «фича как модуль с публичным API».

* **Комбинация**

  * Не заставляет городить фасад там, где реально две строчки логики.
  * Фасады оставляем для «бизнес-сцен» (сложные формы, мастера, страницы).

### Аргументы «против**

* **Прямой доступ**

  * Компоненты привязываются к реализации стейта, их сложнее переиспользовать в других контекстах.
  * Со временем компоненты вырастают и тянут всё больше логики, потому что «ну store же рядом».

* **Фасад**

  * Ещё один слой, лишний бойлерплейт в простых кейсах.
  * Можно легко превратить фасад в **новый God-Service**, если туда тащить всё подряд.

* **Комбинация**

  * Нужно правило: **когда именно** использовать фасад, иначе в проекте хаос.

### Что устарело / плохо

* Компоненты, у которых внутри и HTTP-запросы, и локальный стейт, и NGRX-селекторы, и куча side-effects — это старый, уже сильно критикуемый подход.

### Что новое / подтверждено

* Фасад как **публичный API фичи** — это одно из реально полезных и признанных сообществом решений в фича-ориентированных/DDD-подходах.
  Не «мода», а ответ на боль чрезмерно жирных компонентов.

### Вопросы

* Как задать **размер фасада**, чтобы он не превратился в «второй God-Service»?
* Нужен ли **один фасад на фичу**, или допустимо иметь подфасады (`OrderListFacade`, `OrderDetailsFacade`)?
* Как фасад стыкуется с **router-уровнем** и модальными флоу?

---

## 6. Глубина внутри фичи: минимализм vs «три слоя» (ui/domain/data-access)

### Варианты

1. **Минималистичный подход:** `components/ + services/` и всё.
2. **Двухслойный:** `ui/ + data-access/` (без отдельно выделенного domain).
3. **Трёхслойный:** `ui/ + domain/ + data-access/` (как в твоей гибридной схеме).
4. Ещё более сложный (добавляются `widgets/`, `entities/`, `features/` внутри фичи — уже из мира feature-sliced design).

### Аргументы «за»

* **Минимализм**

  * Быстро, понятно, особенно соло-разработчику.
  * Подходит для фич «на 200 строк», которые вряд ли вырастут.

* **2–3 слоя**

  * Чётко разделяет **UI ↔ бизнес ↔ данные**.
  * Легче рефакторить: можно модифицировать domain, не трогая UI и API (и наоборот).
  * Становится опорной схемой для всей команды.

### Аргументы «против**

* **Минимализм**

  * Легко перерастает в кашу по мере роста фичи.
  * Сложно потом «прорезать слои», когда всё уже перемешано.

* **3 слоя**

  * Для маленьких кейсов ощущается как **over-engineering**.
  * Требует дисциплины и onboard’инга: у джунов глаза начинают стекленеть от количества папок.

### Что устарело / плохо

* Подход «всё в `components/` и `services/` навсегда» в больших проектах — уже очевидно токсичен, про это почти нет споров.

### Что новое / подтверждено

* Идея **адаптивной глубины**: маленькая фича может начаться просто с `ui + service`, но структура заранее допускает «расширение до domain/data-access».
  Это как раз свежий, разумный компромисс, который поддерживают те, кто устал от «архитектуры ради архитектуры».

### Вопросы

* Как объективно фиксировать момент, когда фича «созрела» для выделения **domain/**?
* Какие критерии определить, что **слоёв стало слишком много** и пора упростить?
* Нужны ли **разные шаблоны** фич: «simple feature», «complex feature», «core feature»?

---

## 7. `index.ts` как публичный вход фичи

### Варианты

1. Есть **`index.ts`**, который реэкспортирует всё нужное наружу.
2. Нет `index.ts`, импортируем по прямым путям (`../features/order/ui/order-page.component`).

### Аргументы «за»

* **С `index.ts`**

  * Фича видна как **модуль с API**: снаружи ты не знаешь её внутреннего дерева.
  * Легко упаковать, переносить, реэкспортировать (Nx libs, lib-style архитектура).
  * Можно явно ограничивать, *что* мы показываем наружу.

* **Без `index.ts`**

  * Меньше мета-файлов, проще навигация IDE.
  * Джунам меньше непонятных сущностей.

### Аргументы «против**

* **С `index.ts`**

  * Ещё один слой косвенности: IDE-переходы становятся чуть менее прямыми.
  * Если не следить, `index.ts` может превратиться в «реэкспорт всего подряд».

* **Без `index.ts`**

  * Наружные модули начинают лезть в **внутреннюю структуру фичи**, появляется связность по путям.
  * Любое изменение структуры = переписывание импортов по коду.

### Что устарело / плохо

* Жёсткая привязка всего приложения к внутренней структуре фич (импорты глубоко внутрь) — старый паттерн, который усложняет рефакторинг.

### Что новое / подтверждено

* `index.ts` как **public API фичи/либы** — де-факто стандарт, особенно в экосистемах, где есть mono-repos, libs и т.п.

### Вопросы

* Где граница между **internal** и **public** внутри фичи, что реально стоит реэкспортить?
* Делать ли **несколько entry-points** (`ui`, `testing`, `mocks`) или один общий `index.ts`?
* Как синхронизировать `index.ts` с реальными потребностями (чтобы не реэкспортить лишнего)?

---

## 8. «Старые» паттерны, которые нейросети, скорее всего, ругали

(По косвенным признакам из текста и общей ситуации)

* **Гигантский `shared/` или `core/` с `services`, `models`, `utils`, `components`, куда тащат всё.**
* **Компоненты-монолиты**, где и HTTP, и бизнес-логика, и вёрстка, и стейт.
* **Redux-образные схемы с тоннами бойлерплейта** для любого чиха.
* **Глобальный app-level store** без фича-изолированности.
* **Сильная зависимость от Angular-модулей** (NgModule-центричность), хотя мир уже ушёл в standalone + feature-first.

---

## 9. Вещи, которые можно считать «реально новым достижением»

То, на чём сейчас довольно большое комьюнити в целом сходится (с поправкой на лагеря):

* **Feature-first структура**: фича — минимальная единица архитектуры.
* **Co-location всего, что относится к фиче** (UI, domain, data-access, tests, mocks) в одной папке.
* **Чёткий раздел UI / Domain / Data-access**, хотя глубина и строгість зависят от размера фичи.
* **Фасады** как публичный API фичи и защита компонент от деталей stаte-слоя.
* **Локализация state на уровне фич**, сколь возможно; минимум «глобального супа».
* **Адаптивная архитектура**: разные фичи могут иметь разную глубину слоя, и это нормально.

---

Если кратко: картина уже понятна — ты собрал мнения, которые описывают **одну и ту же плоскость спорных решений**, просто с разным весом акцентов.
Я специально **не делал вывод**, какой вариант «правильный», а вытащил оси выбора, аргументы и вопросы.

Дальше можно идти по аспектам, которые тебе сейчас наиболее живые (например, фасад vs прямой store, или где именно резать domain/data-access), и уже добивать до конкретных правил под твой стиль и твой стэк.










Теперь помогаю тебе определиться как правильно - делаем архитектуру под большие и очень большие (может даже огромные) проекты. Тоесть архитектура нужна по максимуму. Учти то что уже понятно как лучшие и правильные подходы. А по тем вопросам которые пока что не понятны, поищи в инете и еще самостоятельно подумай. И дай мне эту структуру и аргументируй почему так









Ок, включаем режим «энтерпрайз на стероидах» 😄
Дальше — **структура под крупный и очень крупный проект**, без экономии на архитектуре.

Я опираюсь и на наши прошлые выводы, и на то, что сейчас пишут/делают Angular-/Nx-/FSD-люди. ([DEV Community][1])

---

## 1. Контекст: что мы считаем оптимумом для огромного проекта

Для реально больших систем уже есть консенсус:

* **Фича-ориентированность + домены** (users, orders, billing…), а не «components/services папки». ([DEV Community][1])
* **Жёсткие границы** между слоями: UI ↔ Domain ↔ Data-Access ↔ State. ([itnext.io][2])
* **Фасад** как публичный API фичи (особенно при NgRx/SignalStore). ([Medium][3])
* **Co-location**: всё, что относится к фиче, лежит рядом (ui, domain, data-access, state, tests). ([feature-sliced.github.io][4])
* **Nx/monorepo или аналог** для доменов/либ — де-факто стандарт для больших фронтов. ([Nx][5])

Глобальные `shared/models`, «компонентный» срач и жирные компоненты с HTTP внутри — это уже считается токсичной старой школой. ([GitHub][6])

---

## 2. Структура на уровне workspace (крупный проект)

Я бы целился в такую общую картинку (Nx-стиль, но можно и без Nx, с поправками):

```text
apps/
  web-app/                # shell, маршруты верхнего уровня, layout
  admin-app/              # если нужно отдельное приложение и т.д.

libs/
  shared/
    ui/                   # чистые, переиспользуемые примитивы (Button, Modal...)
    utils/                # чистые функции, форматтеры
    config/               # токены конфигурации, environment, feature-flags
    api/                  # низкоуровневые HTTP-клиенты, interceptors

  domains/
    cleaning/
      order/              # <<< НАША ФИЧА (или доменная саб-фича)
      subscription/
      ...

    users/
      profile/
      auth/
      ...
```

Дальше углубляемся в **одну фичу** — `cleaning/order/`.

---

## 3. Структура фичи «по максимуму»

Предлагаю такой скелет:

```text
libs/domains/cleaning/order/
  index.ts                     # публичный API фичи
  cleaning-order.routes.ts     # маршруты фичи

  ui/
    pages/
      cleaning-order-page.component.ts
      cleaning-order-history-page.component.ts
    widgets/
      cleaning-order-form/
        cleaning-order-form.component.ts
        cleaning-order-form.model.ts      # чисто UI-модель формы
    components/
      cleaning-order-summary.component.ts
      cleaning-order-status-badge.component.ts

  domain/
    models/
      cleaning-order.model.ts             # сущности домена
      cleaning-type.enum.ts
    value-objects/
      address.vo.ts
      money.vo.ts
      timeslot.vo.ts
    rules/
      cleaning-order.rules.ts             # расчёт цены, валидаторы, смена статусов
    services/
      cleaning-order.domain-service.ts    # оркестрация доменных правил
    errors/
      cleaning-order.errors.ts            # доменные ошибки / result-типы

  data-access/
    api/
      cleaning-order.api.ts               # обращения к backend
      cleaning-order.endpoints.ts         # URL, query-параметры
    dto/
      cleaning-order.dto.ts               # DTO от/к backend
    mappers/
      cleaning-order.mapper.ts            # DTO <-> domain/models
    mocks/
      cleaning-order.mocks.ts             # фейковые ответы, контракт-тесты

  state/
    cleaning-order.store.ts               # SignalStore или NgRx slice
    cleaning-order.facade.ts              # фасад: публичные методы/стримы
    cleaning-order.effects.ts             # если используем NgRx effects
    cleaning-order.selectors.ts           # если селекторы выносятся отдельно

  testing/
    cleaning-order.fixtures.ts            # тестовые данные, билдеры
    cleaning-order.spec.ts                # интеграционные тесты фичи
```

Теперь — **почему именно так**.

---

## 4. Почему `ui/` так детализирован: pages / widgets / components

### Что мы выигрываем

* **`pages/`** = только **роутируемые контейнеры**. Они знают:

  * как читать URL, query, route-params,
  * как дернуть фасад/стор,
  * как собрать страницу из виджетов.
* **`widgets/`** = самодостаточные куски UI с логикой уровня «мини-фичи» (форма заказа, список заказов).
* **`components/`** = максимально «глупые» и переиспользуемые кирпичики внутри фичи.

Такой слой — прямое продолжение идей FSD (`pages / features / entities`) и современных гайдов по Angular v20+, где страницы выделяются отдельно, чтобы не смешивать «страничную» и переиспользуемую логику. ([feature-sliced.design][7])

### Почему это хорошо именно для огромных проектов

* В больших системах очень быстро плодятся разные версии страниц (web, admin, mobile-layout), но **виджеты и компоненты могут быть общими**.
* Разделение `pages/widgets/components` позволяет:

  * безопасно переиспользовать виджеты между страницами,
  * понимать, где «границы маршрутизации», а где просто UI.

---

## 5. Почему жёстко выделяем `domain/`

### Идея

`domain/` — это **чистый бизнес-слой**, который:

* не знает ни про Angular, ни про HTTP, ни про формы,
* оперирует сущностями/значениями/правилами.

Это ровно тот подход, который описывают как **Clean Architecture / Domain layer** для Angular: бизнес-правила отделяются от фреймворка и транспорта. ([Stackademic][8])

### Что это даёт на гигантском проекте

* Можно переписывать UI (Angular → Web Components → другой фронт) **без переписи доменной логики**.
* Можно жить с несколькими backend-ами (микросервисы, разделённые API) и строить свой нормальный домен поверх кривых DTO.
* Легче писать unit-тесты: тестируем чистые функции/сервисы без Angular TestBed.

### Почему модели/VO/правила разделены

* `models/` — базовые сущности.
* `value-objects/` — штуки с инвариантами (деньги, даты, слоты времени).
* `rules/` — отдельные чистые функции для расчётов/валидаций.
* `services/` — доменная оркестрация (например, «создать заказ с правилами скидок»).

Так мы избегаем скатывания в «один god-service», что типично для плохих «DDD-на-фронте». ([Angular Space][9])

---

## 6. Почему `data-access/` отдельно и именно так

### Идея

`data-access/` — всё, что связано с **внешними источниками данных**:

* HTTP, WebSocket, IndexedDB, локальный cache, etc.

Современные гайды по Angular/Nx прямо показывают `data-access` как стандартный слой рядом с `ui` и `domain`. ([DEV Community][1])

### Разделение внутри

* `api/` — конкретные функции/классы/ресурсы, которые дергают backend.
* `dto/` — сырые типы транспорта (то, как backend реально отвечает).
* `mappers/` — преобразования DTO ↔ domain.
* `mocks/` — заглушки/ответы для storybook, contract-тестов, offline-режимов.

### Почему важно для огромного проекта

* API будут изменяться **чаще**, чем доменные модели.
  Развод DTO и domain — это страховка от боли.
* Можно **подменить источник данных** (например, временно работать с локальным storage) без трогания домена и UI.
* `mocks/` в рамках фичи позволяют:

  * локально разрабатывать фичу, пока backend ещё не готов,
  * писать e2e/contract тесты стабильно.

---

## 7. Почему отдельный `state/` и фасад

### Что мы кладём в `state/`

* `*.store.ts` — реализация state-менеджмента:

  * NgRx slice (reducer/actions/selectors),
  * или SignalStore/набор signal-based сторах.
* `*.facade.ts` — фасад.
* `*.effects.ts` / `*.selectors.ts` — если идём в классический NgRx.

Фасад-паттерн для NgRx/Angular уже несколько лет описывается как **best practice** именно для больших приложений: UI получает простой интерфейс вместо прямой работы со store/effects. ([Medium][3])

### Почему фасад обязателен в «максимальной» архитектуре

* Компоненту не важно, **NgRx там, signals, Akita или что-то ещё**.
  Она общается через методы фасада:

  * `loadOrder(id)`
  * `placeOrder(dto)`
  * `orders$`, `isLoading$`, `error$` и т.п.
* При миграции стейт-библиотеки меняем реализацию в `state/`, **UI не трогаем**.
* Фасад становится тем самым «публичным backend-ом для UI», локальным BFF слоем на стороне фронта.

---

## 8. `index.ts` как обязательный публичный вход фичи

Для больших монореп/энтерпрайз-проектов уже общепринято иметь **public API для каждой библиотеки/фичи**. Nx, Angular style-guide и многие статьи об этом говорят прямо. ([Nx][5])

В `index.ts` мы реэкспортируем:

* публичные компоненты (страницы/виджеты, которые могут использоваться извне),
* фасад/токены,
* маршруты (`ROUTES` или `provideRoutes([...])`).

Это даёт:

* **инкапсуляцию**: снаружи никто не лазит внутрь `domain/` или `data-access/` напрямую,
* возможность потом вынести фичу в отдельный пакет без переработки импортов.

---

## 9. testing/ рядом с фичей

Для огромного проекта тесты, мок-данные и фикстуры должны лежать **рядом с кодом**, а не где-то в отдельном `tests/`:

* проще находить, что относится к какой фиче,
* проще поддерживать контракт-тесты API (dto + mocks),
* проще новым людям зайти в фичу и увидеть полный контекст.

Это совпадает и с Angular-гайдами, и с общими практиками FSD/DDD front-end. ([angular.dev][10])

---

## 10. Почему это именно «архитектура по максимуму»

1. **Каждый слой имеет свою чёткую ответственность**
   UI / Domain / Data-Access / State / Facade / Testing.

2. **Фича воспринимается как мини-подсистема** с собственным API (`index.ts`, маршруты, фасад).

3. Структура **совместима с Nx + ограничения по зависимостям** (domains, layers), что критично для монорепо. ([Nx][11])

4. Мы сознательно отказываемся от:

   * глобальных `shared/models`,
   * «плоских компонент/сервис» папок,
   * прямого доступа UI → store/api.

5. Внутри каждого слоя мы допускаем **вариативность по реализации** (NgRx vs SignalStore, REST vs GraphQL), но **границы слоёв и структура папок фиксированы** — это и есть «максимальная» архитектура.

---

Если хочешь, дальше можем:

* развернуть пример **конкретно для Angular + Signals + NgRx SignalStore** в этой структуре;
* или пройтись по одной фиче «end-to-end» (маршруты → фасад → store → domain → data-access) и расписать контракт между слоями.

[1]: https://dev.to/xrdev38/10-modern-best-practices-for-structuring-large-scale-angular-applications-2m9d?utm_source=chatgpt.com "10 Modern Best Practices for Structuring Large-Scale ..."
[2]: https://itnext.io/how-to-think-in-frontend-layers-not-just-features-1d2bdc8b9287?utm_source=chatgpt.com "How to Think in Frontend Layers (Not Just Features)"
[3]: https://medium.com/%40sehban.alam/understanding-the-facade-pattern-in-angular-a-step-by-step-guide-bdbf1cac4ec7?utm_source=chatgpt.com "Understanding the Facade Pattern in Angular: A Step-by- ..."
[4]: https://feature-sliced.github.io/documentation/docs/get-started/overview?utm_source=chatgpt.com "Overview | Feature-Sliced Design - GitHub Pages"
[5]: https://nx.dev/docs/technologies/angular/generators?utm_source=chatgpt.com "nx/angular - Generators"
[6]: https://github.com/angular/angular/issues/61157?utm_source=chatgpt.com "folder structure in new style guide proposal · Issue #61157"
[7]: https://feature-sliced.design/docs/reference/layers?utm_source=chatgpt.com "Layers"
[8]: https://blog.stackademic.com/angular-and-clean-architecture-a-comprehensive-introduction-to-domain-and-business-layers-f74f443bfaf2?utm_source=chatgpt.com "Angular & Clean Architecture: Introduction to Domain and ..."
[9]: https://www.angularspace.com/youre-misunderstanding-ddd-in-angular-and-frontend/?utm_source=chatgpt.com "You're misunderstanding DDD in Angular (and Frontend)"
[10]: https://angular.dev/style-guide?utm_source=chatgpt.com "Angular coding style guide"
[11]: https://nx.dev/blog/architecting-angular-applications?utm_source=chatgpt.com "Angular Architecture Guide To Building Maintainable ..."

















Ок, давай прям руками соберём фичу `cleaning/order` под **Angular + Signals + NgRx SignalStore** в том «энтерпрайз по максимуму», о котором говорили.

Я покажу:

1. Дерево фичи
2. Поток данных (UI → Facade → SignalStore → API/Domain → обратно в UI)
3. Ключевые файлы с кодом и комментариями

---

## 1. Дерево фичи под Angular + Signals + SignalStore

```text
libs/domains/cleaning/order/
  index.ts
  cleaning-order.routes.ts

  ui/
    pages/
      cleaning-order-page.component.ts
    widgets/
      cleaning-order-form/
        cleaning-order-form.component.ts
        cleaning-order-form.model.ts
    components/
      cleaning-order-summary.component.ts
      cleaning-order-status-badge.component.ts

  domain/
    models/
      cleaning-order.model.ts
    value-objects/
      address.vo.ts
      money.vo.ts
    rules/
      cleaning-order.rules.ts
    services/
      cleaning-order.domain-service.ts

  data-access/
    api/
      cleaning-order.api.ts
      cleaning-order.endpoints.ts
    dto/
      cleaning-order.dto.ts
    mappers/
      cleaning-order.mapper.ts
    mocks/
      cleaning-order.mocks.ts

  state/
    cleaning-order.state.ts
    cleaning-order.store.ts       # NgRx SignalStore
    cleaning-order.facade.ts

  testing/
    cleaning-order.fixtures.ts
    cleaning-order.spec.ts
```

---

## 2. Поток данных (как всё связано)

* **Страница** `CleaningOrderPageComponent`:

  * инжектит `CleaningOrderFacade`,
  * подписывается на Signals `orders`, `loading`, `error`, `hasOrders`,
  * кидает события `submitOrder` в фасад.

* **Facade**:

  * прокидывает наружу signals из `CleaningOrderStore`,
  * даёт методы `loadOrders()`, `createOrder(draft)` — просто делегирует в store.

* **SignalStore**:

  * хранит state как Signals,
  * методы `loadOrders`, `createOrder` вызывают `CleaningOrderApi`,
  * используют мапперы DTO ↔ domain + доменные правила,
  * через `store.patch()` меняют state.

* **UI**:

  * в шаблоне вызывает `orders()`, `loading()`, `error()` — чистые signals.

---

## 3. Domain-слой (модели, value-objects, правила)

### domain/models/cleaning-order.model.ts

```ts
// domain/models/cleaning-order.model.ts

import { Address } from '../value-objects/address.vo';
import { Money } from '../value-objects/money.vo';

export enum CleaningOrderStatus {
  Draft = 'DRAFT',
  Confirmed = 'CONFIRMED',
  InProgress = 'IN_PROGRESS',
  Done = 'DONE',
  Cancelled = 'CANCELLED',
}

export interface CleaningOrder {
  id: string;
  customerName: string;
  address: Address;
  date: Date;
  hours: number;
  price: Money;
  status: CleaningOrderStatus;
}

export interface CleaningOrderDraft {
  customerName: string;
  address: Address;
  date: Date;
  hours: number;
  cleaningTypeId: string;
}
```

### domain/value-objects/money.vo.ts

```ts
// domain/value-objects/money.vo.ts

export class Money {
  constructor(
    public readonly amount: number,
    public readonly currency: string = 'UAH',
  ) {
    if (amount < 0) {
      throw new Error('Amount must be >= 0');
    }
  }

  add(other: Money): Money {
    if (other.currency !== this.currency) {
      throw new Error('Currencies mismatch');
    }
    return new Money(this.amount + other.amount, this.currency);
  }
}
```

### domain/value-objects/address.vo.ts

```ts
// domain/value-objects/address.vo.ts

export class Address {
  constructor(public readonly raw: string) {
    if (!raw || raw.trim().length < 5) {
      throw new Error('Address is too short');
    }
  }

  toString(): string {
    return this.raw;
  }
}
```

### domain/rules/cleaning-order.rules.ts

```ts
// domain/rules/cleaning-order.rules.ts

import { CleaningOrderDraft } from '../models/cleaning-order.model';
import { Money } from '../value-objects/money.vo';

export function calculateBasePrice(draft: CleaningOrderDraft): Money {
  const baseRatePerHour = 200; // условный тариф
  const amount = draft.hours * baseRatePerHour;
  return new Money(amount, 'UAH');
}
```

---

## 4. Data-access: DTO, мапперы, API

### data-access/dto/cleaning-order.dto.ts

```ts
// data-access/dto/cleaning-order.dto.ts

export interface CleaningOrderDto {
  id: string;
  customer_name: string;
  address: string;
  date: string;             // ISO
  hours: number;
  price: number;
  currency: string;
  status: 'DRAFT' | 'CONFIRMED' | 'IN_PROGRESS' | 'DONE' | 'CANCELLED';
}

export interface CreateCleaningOrderDto {
  customer_name: string;
  address: string;
  date: string;
  hours: number;
  cleaning_type_id: string;
}
```

### data-access/mappers/cleaning-order.mapper.ts

```ts
// data-access/mappers/cleaning-order.mapper.ts

import {
  CleaningOrder,
  CleaningOrderDraft,
  CleaningOrderStatus,
} from '../../domain/models/cleaning-order.model';
import { Money } from '../../domain/value-objects/money.vo';
import { Address } from '../../domain/value-objects/address.vo';
import {
  CleaningOrderDto,
  CreateCleaningOrderDto,
} from '../dto/cleaning-order.dto';

export function mapDtoToCleaningOrder(dto: CleaningOrderDto): CleaningOrder {
  return {
    id: dto.id,
    customerName: dto.customer_name,
    address: new Address(dto.address),
    date: new Date(dto.date),
    hours: dto.hours,
    price: new Money(dto.price, dto.currency),
    status: dto.status as CleaningOrderStatus,
  };
}

export function mapDraftToCreateDto(draft: CleaningOrderDraft): CreateCleaningOrderDto {
  return {
    customer_name: draft.customerName,
    address: draft.address.toString(),
    date: draft.date.toISOString(),
    hours: draft.hours,
    cleaning_type_id: draft.cleaningTypeId,
  };
}
```

### data-access/api/cleaning-order.api.ts

```ts
// data-access/api/cleaning-order.api.ts

import { Injectable, inject } from '@angular/core';
import { HttpClient } from '@angular/common/http';
import { Observable } from 'rxjs';
import {
  CleaningOrderDto,
  CreateCleaningOrderDto,
} from '../dto/cleaning-order.dto';

@Injectable({ providedIn: 'root' }) // или provideIn фичи
export class CleaningOrderApi {
  private readonly http = inject(HttpClient);
  private readonly baseUrl = '/api/cleaning-orders';

  getOrders(): Observable<CleaningOrderDto[]> {
    return this.http.get<CleaningOrderDto[]>(this.baseUrl);
  }

  createOrder(dto: CreateCleaningOrderDto): Observable<CleaningOrderDto> {
    return this.http.post<CleaningOrderDto>(this.baseUrl, dto);
  }
}
```

---

## 5. State: тип, SignalStore, Facade

### state/cleaning-order.state.ts

```ts
// state/cleaning-order.state.ts

import { CleaningOrder } from '../domain/models/cleaning-order.model';

export interface CleaningOrderState {
  orders: CleaningOrder[];
  loading: boolean;
  error: string | null;
}

export const initialCleaningOrderState: CleaningOrderState = {
  orders: [],
  loading: false,
  error: null,
};
```

### state/cleaning-order.store.ts (NgRx SignalStore)

```ts
// state/cleaning-order.store.ts

import { computed, inject } from '@angular/core';
import {
  signalStore,
  withState,
  withMethods,
  withComputed,
} from '@ngrx/signals';

import {
  CleaningOrderState,
  initialCleaningOrderState,
} from './cleaning-order.state';
import { CleaningOrderApi } from '../data-access/api/cleaning-order.api';
import {
  mapDtoToCleaningOrder,
  mapDraftToCreateDto,
} from '../data-access/mappers/cleaning-order.mapper';
import { CleaningOrderDraft } from '../domain/models/cleaning-order.model';
import { tap } from 'rxjs/operators';

export const CleaningOrderStore = signalStore(
  // тут можно указать providedIn:'root' или оставить без и провайдить в фиче
  { providedIn: 'root' },
  withState<CleaningOrderState>(initialCleaningOrderState),

  withComputed((store) => ({
    hasOrders: computed(() => store.orders().length > 0),
  })),

  withMethods((store) => {
    const api = inject(CleaningOrderApi);

    return {
      loadOrders() {
        store.patch({ loading: true, error: null });

        api
          .getOrders()
          .pipe(
            tap({
              next: (dtos) => {
                store.patch({
                  orders: dtos.map(mapDtoToCleaningOrder),
                  loading: false,
                });
              },
              error: (err: unknown) => {
                const message =
                  err instanceof Error ? err.message : 'Не удалось загрузить заказы';
                store.patch({ error: message, loading: false });
              },
            })
          )
          .subscribe();
      },

      createOrder(draft: CleaningOrderDraft) {
        store.patch({ loading: true, error: null });

        const dto = mapDraftToCreateDto(draft);

        api
          .createOrder(dto)
          .pipe(
            tap({
              next: (createdDto) => {
                const created = mapDtoToCleaningOrder(createdDto);
                store.patch({
                  orders: [...store.orders(), created],
                  loading: false,
                });
              },
              error: (err: unknown) => {
                const message =
                  err instanceof Error ? err.message : 'Не удалось создать заказ';
                store.patch({ error: message, loading: false });
              },
            })
          )
          .subscribe();
      },
    };
  })
);
```

**Смысл:**

* `withState` создаёт signals `orders`, `loading`, `error`.
* `withComputed` даёт производный signal `hasOrders`.
* `withMethods` инжектит API и возвращает методы, которые меняют state через `patch` и вызывают HTTP.

### state/cleaning-order.facade.ts

```ts
// state/cleaning-order.facade.ts

import { Injectable, inject } from '@angular/core';
import { CleaningOrderStore } from './cleaning-order.store';
import { CleaningOrderDraft } from '../domain/models/cleaning-order.model';

@Injectable({ providedIn: 'root' })
export class CleaningOrderFacade {
  private readonly store = inject(CleaningOrderStore);

  // signals — тупо прокидываем наружу
  readonly orders = this.store.orders;
  readonly loading = this.store.loading;
  readonly error = this.store.error;
  readonly hasOrders = this.store.hasOrders;

  loadOrders(): void {
    this.store.loadOrders();
  }

  createOrder(draft: CleaningOrderDraft): void {
    this.store.createOrder(draft);
  }
}
```

**Зачем фасад:**

* Компоненты вообще не знают про `signalStore` и внутреннюю реализацию.
* Можно заменить SignalStore на другой механизм, не трогая UI.
* Фасад — это API фичи для UI-слоя.

---

## 6. UI-слой: страница + форма

### ui/widgets/cleaning-order-form/cleaning-order-form.model.ts

```ts
// ui/widgets/cleaning-order-form/cleaning-order-form.model.ts

import { Address } from '../../../domain/value-objects/address.vo';

export interface CleaningOrderFormValue {
  customerName: string;
  address: string;
  date: string;  // ISO из input[type=date] + time
  hours: number;
  cleaningTypeId: string;
}

export function toDraft(form: CleaningOrderFormValue) {
  return {
    customerName: form.customerName,
    address: new Address(form.address),
    date: new Date(form.date),
    hours: form.hours,
    cleaningTypeId: form.cleaningTypeId,
  };
}
```

### ui/widgets/cleaning-order-form/cleaning-order-form.component.ts

```ts
// ui/widgets/cleaning-order-form/cleaning-order-form.component.ts

import {
  Component,
  EventEmitter,
  Output,
} from '@angular/core';
import { NgIf } from '@angular/common';
import { FormsModule } from '@angular/forms';
import {
  CleaningOrderFormValue,
  toDraft,
} from './cleaning-order-form.model';
import { CleaningOrderDraft } from '../../../domain/models/cleaning-order.model';

@Component({
  standalone: true,
  selector: 'app-cleaning-order-form',
  imports: [NgIf, FormsModule],
  template: `
    <form (ngSubmit)="onSubmit()" #f="ngForm">
      <label>
        Имя клиента
        <input name="customerName" [(ngModel)]="model.customerName" required />
      </label>

      <label>
        Адрес
        <input name="address" [(ngModel)]="model.address" required />
      </label>

      <label>
        Дата
        <input type="datetime-local" name="date" [(ngModel)]="model.date" required />
      </label>

      <label>
        Часы
        <input type="number" name="hours" [(ngModel)]="model.hours" min="1" required />
      </label>

      <label>
        Тип уборки
        <select name="cleaningTypeId" [(ngModel)]="model.cleaningTypeId" required>
          <option value="standard">Стандартная</option>
          <option value="deep">Глубокая</option>
        </select>
      </label>

      <button type="submit" [disabled]="!f.form.valid">
        Оформить заказ
      </button>
    </form>
  `,
})
export class CleaningOrderFormComponent {
  @Output() submitOrder = new EventEmitter<CleaningOrderDraft>();

  model: CleaningOrderFormValue = {
    customerName: '',
    address: '',
    date: '',
    hours: 2,
    cleaningTypeId: 'standard',
  };

  onSubmit() {
    const draft = toDraft(this.model);
    this.submitOrder.emit(draft);
  }
}
```

### ui/pages/cleaning-order-page.component.ts

```ts
// ui/pages/cleaning-order-page.component.ts

import { Component, Signal, inject } from '@angular/core';
import { NgIf, NgFor } from '@angular/common';
import { CleaningOrderFacade } from '../../state/cleaning-order.facade';
import {
  CleaningOrder,
  CleaningOrderDraft,
} from '../../domain/models/cleaning-order.model';
import { CleaningOrderFormComponent } from '../widgets/cleaning-order-form/cleaning-order-form.component';

@Component({
  standalone: true,
  selector: 'app-cleaning-order-page',
  imports: [NgIf, NgFor, CleaningOrderFormComponent],
  template: `
    <section>
      <h1>Заказ уборки</h1>

      <app-cleaning-order-form
        (submitOrder)="onSubmit($event)">
      </app-cleaning-order-form>

      <div *ngIf="loading()">Загрузка...</div>
      <div *ngIf="error() as e" class="error">{{ e }}</div>

      <ul *ngIf="hasOrders()">
        <li *ngFor="let order of orders()">
          {{ order.customerName }} —
          {{ order.price.amount }} {{ order.price.currency }}
        </li>
      </ul>
    </section>
  `,
})
export class CleaningOrderPageComponent {
  private readonly facade = inject(CleaningOrderFacade);

  readonly orders: Signal<CleaningOrder[]> = this.facade.orders;
  readonly loading = this.facade.loading;
  readonly error = this.facade.error;
  readonly hasOrders = this.facade.hasOrders;

  ngOnInit() {
    this.facade.loadOrders();
  }

  onSubmit(draft: CleaningOrderDraft) {
    this.facade.createOrder(draft);
  }
}
```

---

## 7. Routes + index.ts

### cleaning-order.routes.ts

```ts
// cleaning-order.routes.ts

import { Routes } from '@angular/router';
import { CleaningOrderPageComponent } from './ui/pages/cleaning-order-page.component';

export const CLEANING_ORDER_ROUTES: Routes = [
  {
    path: '',
    component: CleaningOrderPageComponent,
  },
];
```

### index.ts (public API фичи)

```ts
// index.ts

export * from './cleaning-order.routes';
export * from './state/cleaning-order.facade';
export * from './domain/models/cleaning-order.model';
```

Использование в app:

```ts
// apps/web-app/src/app/app.routes.ts

import { Routes } from '@angular/router';

export const APP_ROUTES: Routes = [
  {
    path: 'cleaning/order',
    loadChildren: () =>
      import('@myorg/domains/cleaning/order').then(
        (m) => m.CLEANING_ORDER_ROUTES,
      ),
  },
];
```

---

## 8. Почему это хорошо именно под Angular + Signals + SignalStore

* **Signals**:

  * всё состояние в `CleaningOrderStore` — это signals (`orders`, `loading`, `error`, `hasOrders`);
  * UI просто дергает `signal()` в шаблоне, без RxJS/async.

* **NgRx SignalStore**:

  * даёт структурированный способ описать state (`withState`), derived state (`withComputed`) и методы (`withMethods`),
  * side-effects (HTTP) живут рядом с изменением state, но не в компонентах.

* **Facade**:

  * UI вообще не знает, что под капотом SignalStore / NgRx,
  * можно переключиться на другой store-мех (даже чистые Angular signals + сервис) без рефакторинга UI.

* **DDD-слои**:

  * domain (модели/VO/правила) чистые, не зависят от Angular,
  * data-access чётко отделён (DTO, мапперы, API),
  * state-слой не проваливается в HTTP-детали за пределами своих задач.

Если захочешь — дальше можем:

* добавить **ещё один сценарий** (например, отмену/изменение заказа) и проследить, как он проходит через все слои,
* или обсудить, как это всё адаптировать под монорепо Nx с жёсткими зависимостями между libs (domains/ui/utils и т.д.).
















Сделаем по-взрослому, под **Nx + Angular + Signals + SignalStore**.
Я дам тебе именно **команды**, без воды. Ты сможешь их прогнать и получить почти тот же скелет, что мы рисовали.

Ниже будет:

1. Создание доменной либы `domains/cleaning/order`
2. Генерация компонентов (page + form + ui)
3. Генерация сервисов (API, facade)
4. Создание остальных файлов через mkdir + пустые ts

Я буду писать команды в **bash-стиле** (`mkdir -p`, `touch`).
В PowerShell это либо через Git Bash, либо заменишь на `New-Item`/`mkdir`.

---

## 1. Создаём доменную либу под фичу

Из корня Nx-workspace:

```bash
npx nx g @nx/angular:library order \
  --directory=domains/cleaning \
  --standalone \
  --routing=false \
  --changeDetection=OnPush \
  --style=scss
```

Это создаст:

```text
libs/domains/cleaning/order/
  src/
    index.ts
    lib/
      order.component.ts   # пока можно удалить/переписать
      ...
  project.json
  ...
```

Мы дальше будем работать внутри:

```bash
cd libs/domains/cleaning/order/src/lib
```

---

## 2. Структура папок фичи

Создаём все каталоги одним махом:

```bash
mkdir -p \
  ui/pages \
  ui/widgets/cleaning-order-form \
  ui/components \
  domain/models \
  domain/value-objects \
  domain/rules \
  domain/services \
  domain/errors \
  data-access/api \
  data-access/dto \
  data-access/mappers \
  data-access/mocks \
  state \
  testing
```

---

## 3. Генерим Angular-компоненты (страница + форма + парочка ui)

> Важно: команды запускаем из корня workspace, указываем проект `domains-cleaning-order`.

```bash
# Страница (route-level контейнер)
npx nx g @nx/angular:component ui/pages/cleaning-order-page \
  --project=domains-cleaning-order \
  --standalone \
  --flat=false \
  --changeDetection=OnPush

# Виджет формы заказа
npx nx g @nx/angular:component ui/widgets/cleaning-order-form \
  --project=domains-cleaning-order \
  --standalone \
  --flat=false \
  --changeDetection=OnPush

# Компонент-сводка
npx nx g @nx/angular:component ui/components/cleaning-order-summary \
  --project=domains-cleaning-order \
  --standalone \
  --flat=false \
  --changeDetection=OnPush

# Бейдж статуса
npx nx g @nx/angular:component ui/components/cleaning-order-status-badge \
  --project=domains-cleaning-order \
  --standalone \
  --flat=false \
  --changeDetection=OnPush
```

Nx положит их в уже созданные нами папки.

---

## 4. Генерим сервисы: API и фасад

```bash
# API-сервис для HTTP
npx nx g @nx/angular:service data-access/api/cleaning-order-api \
  --project=domains-cleaning-order \
  --flat=true

# Facade для стейта
npx nx g @nx/angular:service state/cleaning-order-facade \
  --project=domains-cleaning-order \
  --flat=true
```

Потом ты просто переименуешь классы в `CleaningOrderApi` и `CleaningOrderFacade` и допишешь код.

---

## 5. Создаём остальные файлы (models, dto, store и т.д.)

Теперь добиваем вручную пустые файлы, чтобы структура была полной:

```bash
cd libs/domains/cleaning/order/src/lib

# routes + корневой index фичи (локальный)
touch cleaning-order.routes.ts

# DOMAIN
touch domain/models/cleaning-order.model.ts
touch domain/value-objects/address.vo.ts
touch domain/value-objects/money.vo.ts
touch domain/rules/cleaning-order.rules.ts
touch domain/services/cleaning-order.domain-service.ts
touch domain/errors/cleaning-order.errors.ts

# DATA-ACCESS
touch data-access/dto/cleaning-order.dto.ts
touch data-access/mappers/cleaning-order.mapper.ts
touch data-access/api/cleaning-order.endpoints.ts
touch data-access/mocks/cleaning-order.mocks.ts

# STATE
touch state/cleaning-order.state.ts
touch state/cleaning-order.store.ts

# UI-модель формы
touch ui/widgets/cleaning-order-form/cleaning-order-form.model.ts

# TESTING
touch testing/cleaning-order.fixtures.ts
touch testing/cleaning-order.spec.ts
```

> Глобальный `src/index.ts`, который Nx создал при генерации либы, ты используешь как **public API** фичи: там потом экспортнёшь `CLEANING_ORDER_ROUTES`, фасад, доменные модели и т.п.

---

## 6. (Необязательно, но удобно) Чистим лишнее от генерации либы

Nx сгенерировал стартовый `order.component.ts` (или похожий).
Его можно удалить или переиспользовать как какой-нибудь `debug`-компонент.

```bash
rm libs/domains/cleaning/order/src/lib/order.component.ts
rm libs/domains/cleaning/order/src/lib/order.component.spec.ts
```

---

## 7. Быстрый чек: что должно получиться (кратко)

В итоге под `libs/domains/cleaning/order/src/lib` ты увидишь:

```text
cleaning-order.routes.ts

ui/
  pages/cleaning-order-page/...
  widgets/cleaning-order-form/...
  components/cleaning-order-summary/...
  components/cleaning-order-status-badge/...

domain/
  models/cleaning-order.model.ts
  value-objects/address.vo.ts
  value-objects/money.vo.ts
  rules/cleaning-order.rules.ts
  services/cleaning-order.domain-service.ts
  errors/cleaning-order.errors.ts

data-access/
  api/cleaning-order-api.service.ts
  api/cleaning-order.endpoints.ts
  dto/cleaning-order.dto.ts
  mappers/cleaning-order.mapper.ts
  mocks/cleaning-order.mocks.ts

state/
  cleaning-order.state.ts
  cleaning-order.store.ts
  cleaning-order-facade.service.ts

testing/
  cleaning-order.fixtures.ts
  cleaning-order.spec.ts
```

Если хочешь, следующим шагом могу:

* дать **минимальный контент** для каждого пустого файла (state, routes, index.ts),
* или собрать **готовый паттерн команд** для генерации следующей фичи по этому же шаблону (чтобы ты мог просто менять имя домена/фичи).












а теперь пройдись по одной фиче «end-to-end» (маршруты → фасад → store → domain → data-access) и расписать контракт между слоями.














Давай пройдём одну фичу **сквозняком**:
кейсы возьмём два — **загрузить список заказов** и **создать заказ**.

Фича: `cleaning/order`
Стек: **Angular + Router + Facade + NgRx SignalStore + Domain + Data-access**

Я не буду снова заливать весь код, а вытащу главное — **что кто кому обещает** (контракт между слоями) и **какие типы ходят туда-сюда**.

---

## 0. Общая картинка потока

**Кейс 1: Загрузить заказы**

`Маршрут → Страница → Фасад → Store → Api + Mapper → DTO ← Api ← Backend`

**Кейс 2: Создать заказ**

`Страница (форма) → Фасад → Store → Mapper → Api → Backend → DTO → Mapper → Store → Фасад → Страница`

По слоям:

1. **Маршруты / Router** — решают *какую* страницу показать.
2. **UI (страница + виджеты)** — показывают данные, собирают ввод, дергают фасад.
3. **Facade** — один публичный интерфейс фичи для UI.
4. **Store (SignalStore)** — хранит состояние фичи, оркестрирует вызовы API и домена.
5. **Domain** — чистые модели и правила.
6. **Data-access** — общается с backend (DTO, HTTP, мапперы).

---

## 1. Контракт: Router → UI (страница)

### Что знает Router

Роуты фичи (`cleaning-order.routes.ts`) знают:

* **URL** и паттерн пути (`path: 'cleaning/order'` или пустой в lazy-модуле),
* **какой компонент** рендерить (`CleaningOrderPageComponent`),
* опционально — **guards/resolvers**.

```ts
export const CLEANING_ORDER_ROUTES: Routes = [
  {
    path: '',
    component: CleaningOrderPageComponent,
  },
];
```

### Контракт

* Router **НЕ знает** ни про домен, ни про API, ни про стейт.
* Он обещает: *“по этому path будет смонтирован такой-то компонент”*.
* Страница обещает: *“я — вход в фичу, я сама разберусь, как грузить данные через фасад”*.

**Стабильность:**
Если мы сменим внутреннюю реализацию фичи (store, API, domain), **router вообще не трогаем**, пока страница та же.

---

## 2. Контракт: UI (страница/виджеты) ↔ Facade

### Страница зависит только от фасада

В компоненте страницы мы инжектим фасад и работаем с **сигналами и методами фасада**:

```ts
export class CleaningOrderPageComponent {
  private readonly facade = inject(CleaningOrderFacade);

  readonly orders = this.facade.orders;      // Signal<CleaningOrder[]>
  readonly loading = this.facade.loading;    // Signal<boolean>
  readonly error = this.facade.error;        // Signal<string | null>
  readonly hasOrders = this.facade.hasOrders;// Signal<boolean>

  ngOnInit() {
    this.facade.loadOrders();
  }

  onSubmit(draft: CleaningOrderDraft) {
    this.facade.createOrder(draft);
  }
}
```

### Явный контракт фасада для UI

Можно даже оформить интерфейсом:

```ts
export interface ICleaningOrderFacade {
  readonly orders: Signal<CleaningOrder[]>;
  readonly loading: Signal<boolean>;
  readonly error: Signal<string | null>;
  readonly hasOrders: Signal<boolean>;

  loadOrders(): void;
  createOrder(draft: CleaningOrderDraft): void;
}
```

**Фасад обязуется:**

* **Не отдавать наружу DTO** — только доменные модели `CleaningOrder`.
* Держать **согласованные сигналы состояния**:
  `loading` корректно отражает загрузку, `error` — последнюю ошибку.
* Методы `loadOrders` / `createOrder`:

  * *не блокируются* (async внутри),
  * не кидают исключения наружу в UI (ошибки — через `error()` или уведомления).

**UI обязуется:**

* Не лезть в store или API напрямую.
* Не мутировать доменные объекты (если это не оговорено отдельно) — работать с ними как с read-only.

**Стабильность:**

* Мы можем:

  * поменять SignalStore на RxJS-сервис,
  * изменить стратегию кеширования,
    — но пока контракт фасада (сигналы + методы) прежний, UI не переписываем.

---

## 3. Контракт: Facade ↔ Store (SignalStore)

### Что знает фасад

Фасад — тонкая прослойка:

```ts
@Injectable({ providedIn: 'root' })
export class CleaningOrderFacade implements ICleaningOrderFacade {
  private readonly store = inject(CleaningOrderStore);

  readonly orders = this.store.orders;
  readonly loading = this.store.loading;
  readonly error = this.store.error;
  readonly hasOrders = this.store.hasOrders;

  loadOrders() {
    this.store.loadOrders();
  }

  createOrder(draft: CleaningOrderDraft) {
    this.store.createOrder(draft);
  }
}
```

### Контракт между фасадом и store

Условный интерфейс store:

```ts
export interface ICleaningOrderStore {
  // state signals
  readonly orders: Signal<CleaningOrder[]>;
  readonly loading: Signal<boolean>;
  readonly error: Signal<string | null>;
  readonly hasOrders: Signal<boolean>;

  // actions
  loadOrders(): void;
  createOrder(draft: CleaningOrderDraft): void;
}
```

**Store обязуется:**

* **Хранить** состояние фичи в signals, не ломая типы (orders всегда массив, error либо строка, либо null).
* **Выполнить** семантику методов:

  * `loadOrders()` → загрузить текущие заказы, корректно выставить `loading/error/orders`,
  * `createOrder(draft)` → отправить draft, добавить созданный заказ в `orders` или выставить `error`.

**Фасад обязуется:**

* Не вмешиваться в логику переходов состояний, только **пробрасывать** методы и сигналы.
* Не внедрять в себя domain и API напрямую — всё через store.

**Стабильность:**

* Если мы захотим добавить, например, **пагинацию**, мы можем:

  * расширить store (новые signals/methods),
  * добавить в фасад новые поля/методы,
    не трогая UI, пока UI их не использует.

---

## 4. Контракт: Store ↔ Domain

### Что делает Store с доменом

В `CleaningOrderStore` есть методы, где:

* он вызывает **domain-правила** (`calculateBasePrice`, валидаторы и т.п.),
* создаёт/обновляет доменные объекты `CleaningOrder`.

Условно:

```ts
import { calculateBasePrice } from '../domain/rules/cleaning-order.rules';
import { CleaningOrderDraft } from '../domain/models/cleaning-order.model';

createOrder(draft: CleaningOrderDraft) {
  // (опционально) store может использовать доменные правила ДО отправки:
  const price = calculateBasePrice(draft);
  // ... или он отдаёт на бэк и доверяет ему — это уже архитектурное решение
}
```

### Контракт со стороны Domain

Domain даёт **чистые функции и типы**, не зависящие от Angular:

```ts
export function calculateBasePrice(draft: CleaningOrderDraft): Money;
export class Money { /* инварианты */ }
export class Address { /* валидация */ }
export interface CleaningOrder { /* сущность */ }
```

**Domain обязуется:**

* Не знать ничего о HTTP, Router, Angular, SignalStore.
* Гарантировать **инварианты**:

  * `Money` не может быть с отрицательной суммой,
  * `Address` не может быть пустым/слишком коротким, и т.п.
* Вести себя как чистый JS/TS-слой:
  — легко тестируемый, без side-effects (кроме выброса ошибок/Result типов).

**Store обязуется:**

* Не ломать доменные инварианты:

  * не создавать `Money` с отрицательной суммой,
  * не обходить валидаторы (если они есть) через `as any`.
* Не протаскивать в domain **сырой DTO** — только через адаптеры/модели.

**Стабильность:**

* Можно менять внутреннюю реализацию правил (`calculateBasePrice`), не трогая store API.
* Можно расширять domain новыми value-objects, пока контракты функций/интерфейсов не ломаются.

---

## 5. Контракт: Store ↔ Data-access

### Что Store просит у data-access

Store не хочет знать, как выглядят HTTP-URL и DTO внутри — он зовёт API-сервис:

```ts
const api = inject(CleaningOrderApi);

api.getOrders(): Observable<CleaningOrderDto[]>;
api.createOrder(dto: CreateCleaningOrderDto): Observable<CleaningOrderDto>;
```

Он берёт DTO и гоняет их через мапперы:

```ts
api.getOrders().pipe(
  tap(dtos => {
    store.patch({
      orders: dtos.map(mapDtoToCleaningOrder),
      loading: false,
    });
  })
)
```

### Контракты со стороны data-access

* **API-сервис**:

```ts
export interface ICleaningOrderApi {
  getOrders(): Observable<CleaningOrderDto[]>;
  createOrder(dto: CreateCleaningOrderDto): Observable<CleaningOrderDto>;
}
```

* **DTO**:

```ts
export interface CleaningOrderDto {
  id: string;
  customer_name: string;
  address: string;
  date: string; // ISO
  hours: number;
  price: number;
  currency: string;
  status: 'DRAFT' | 'CONFIRMED' | 'IN_PROGRESS' | 'DONE' | 'CANCELLED';
}
```

* **Мапперы**:

```ts
export function mapDtoToCleaningOrder(dto: CleaningOrderDto): CleaningOrder;
export function mapDraftToCreateDto(draft: CleaningOrderDraft): CreateCleaningOrderDto;
```

**Data-access обязуется:**

* Держать **контракт с backend**:

  * пути, методы, структуры DTO.
* Не протаскивать DTO наружу в UI — только доменные модели после маппинга.
* Не тянуть Angular-specific штуки в мапперы (мапперы должны быть чистыми).

**Store обязуется:**

* Не использовать DTO напрямую в state, только доменные модели.
* Правильно интерпретировать ошибки из API:

  * конвертировать их в строки/доменные ошибки, которые можно показать в UI.

**Стабильность:**

* Если меняется backend (например, поле `customer_name` → `client_name`), мы правим:

  * DTO-интерфейс,
  * реализацию API-сервиса,
  * мапперы.
    Store и выше — не трогаем, пока маппер сохраняет тот же доменный контракт.

---

## 6. Сквозной пример: «Загрузить список заказов»

Разложим по шагам и контрактам.

1. **Переход по маршруту** `/cleaning/order`

   * Router монтирует `CleaningOrderPageComponent`.
2. **Страница в `ngOnInit` вызывает** `facade.loadOrders()`

   * Контракт: фасад обязан инициировать загрузку, не падая исключением.
3. **Facade делегирует в Store** → `store.loadOrders()`
4. **Store:**

   * выставляет `loading = true`, `error = null`,
   * вызывает `api.getOrders()`,
   * получает `CleaningOrderDto[]`,
   * через `mapDtoToCleaningOrder` превращает их в `CleaningOrder[]`,
   * кладёт в state `orders = [...]`, `loading = false`.
5. **UI:**

   * через signals получает обновление `orders()`, `loading()`,
   * рисует список.

Если что-то падает:

* API бросает ошибку → Store ловит и пишет в `error` текст (или доменную ошибку), `loading = false`.
* Страница отображает `error()`.

Нигде выше DTO не вылезают, UI не знает про HTTP.

---

## 7. Сквозной пример: «Создать новый заказ»

1. Пользователь заполняет форму `CleaningOrderFormComponent`.
2. Форма эмитит `submitOrder(draft: CleaningOrderDraft)` вверх.
3. Страница вызывает `facade.createOrder(draft)`.
4. Facade → `store.createOrder(draft)`.
5. Store:

   * (опционально) проверяет / обогащает draft через domain-правила,
   * делает `mapDraftToCreateDto(draft)` → `CreateCleaningOrderDto`,
   * вызывает `api.createOrder(dto)`,
   * получает `CleaningOrderDto`,
   * делает `mapDtoToCleaningOrder(dto)` → `CleaningOrder`,
   * через `patch` добавляет заказ в `orders` и сбрасывает `loading/error`.

UI обновляется автоматически по signals.

---

## 8. Зачем вообще вся эта контрактность

Для **огромного проекта** это даёт:

* Возможность **менять любую внутреннюю реализацию** по слоям, не разламывая всё дерево:

  * поменяли API → только data-access и мапперы;
  * поменяли бизнес-правила → только domain;
  * решили уйти с NgRx SignalStore на другую lib → store + фасад (внутри), UI не трогаем.
* Чёткие точки обзора:

  * UI-дев смотрит только на фасад и доменные модели;
  * backend-интегратор — на DTO и API;
  * доменный архитектор — на `domain/*`.

---

Если хочешь, дальше можем:

* формализовать это в виде **чистых TS-интерфейсов** `IXXXFacade`, `IXXXStore`, `IXXXApi`, `IDomainRule` — как отдельный артефакт «контракт фичи»;
* или взять другую фичу (не заказ, а, например, `user/profile`) и сделать тот же разбор, чтобы увидеть, как повторяется паттерн.














Ок, давай зафиксируем **целый проект целиком** — как если бы ты реально поднял Nx + Angular workspace под нашу «энтерпрайз» архитектуру.

Я покажу структуру:

* без `node_modules`, `dist`, `.angular`, etc — это шум;
* с **ключевыми файлами** и папками, где дальше живёт реальный код;
* с пометками комментариями `#` / `//`.

---

```text
my-workspace/                         # корень Nx-монорепы
├── nx.json
├── package.json
├── pnpm-lock.yaml / yarn.lock / package-lock.json
├── tsconfig.base.json
├── workspace.json           # (если есть, у новых Nx часто всё в project.json)
├── jest.config.ts           # если используешь Jest
├── .editorconfig
├── .gitignore
├── .prettierrc
├── .eslintrc.json

├── apps/
│   ├── web-app/                       # основное Angular-приложение (shell)
│   │   ├── project.json
│   │   ├── tsconfig.app.json
│   │   └── src/
│   │       ├── main.ts
│   │       ├── index.html
│   │       ├── styles.scss
│   │       ├── environments/
│   │       │   ├── environment.ts
│   │       │   └── environment.prod.ts
│   │       └── app/
│   │           ├── app.config.ts      # provideRouter, provideHttpClient, etc
│   │           ├── app.routes.ts      # корневые маршруты приложения
│   │           ├── app.component.ts   # корневой layout (shell)
│   │           ├── app.component.html
│   │           └── app.component.scss
│   │
│   └── admin-app/                     # опционально: отдельная админка
│       ├── project.json
│       └── src/
│           ├── main.ts
│           ├── index.html
│           ├── styles.scss
│           └── app/
│               ├── app.config.ts
│               ├── app.routes.ts
│               └── app.component.ts

├── libs/
│   ├── shared/
│   │   ├── ui/                        # общие UI-примитивы (кнопки, модалки и т.п.)
│   │   │   ├── project.json
│   │   │   └── src/
│   │   │       ├── index.ts          # public API shared-ui
│   │   │       └── lib/
│   │   │           ├── button/
│   │   │           │   ├── button.component.ts
│   │   │           │   ├── button.component.html
│   │   │           │   └── button.component.scss
│   │   │           ├── modal/
│   │   │           └── form-field/
│   │   │               └── ...
│   │   │
│   │   ├── utils/                     # чистые функции, хелперы
│   │   │   ├── project.json
│   │   │   └── src/
│   │   │       ├── index.ts
│   │   │       └── lib/
│   │   │           ├── date/
│   │   │           │   └── date-format.util.ts
│   │   │           ├── money/
│   │   │           │   └── money-format.util.ts
│   │   │           └── ...
│   │   │
│   │   ├── config/                    # конфиги, токены, feature-flags
│   │   │   ├── project.json
│   │   │   └── src/
│   │   │       ├── index.ts
│   │   │       └── lib/
│   │   │           ├── app-config.token.ts
│   │   │           ├── environment-config.ts
│   │   │           └── feature-flags.ts
│   │   │
│   │   └── api/                       # низкоуровневые вещи для HTTP
│   │       ├── project.json
│   │       └── src/
│   │           ├── index.ts
│   │           └── lib/
│   │               ├── http/
│   │               │   ├── http-config.ts
│   │               │   ├── auth.interceptor.ts
│   │               │   └── error.interceptor.ts
│   │               ├── rest-client.ts
│   │               └── api-types.ts
│   │
│   └── domains/
│       ├── cleaning/
│       │   ├── order/                 # НАША ФИЧА cleaning/order
│       │   │   ├── project.json
│       │   │   ├── tsconfig.lib.json
│       │   │   └── src/
│       │   │       ├── index.ts       # public API фичи
│       │   │       └── lib/
│       │   │           ├── cleaning-order.routes.ts
│       │   │           │
│       │   │           ├── ui/
│       │   │           │   ├── pages/
│       │   │           │   │   └── cleaning-order-page/
│       │   │           │   │       ├── cleaning-order-page.component.ts
│       │   │           │   │       ├── cleaning-order-page.component.html
│       │   │           │   │       └── cleaning-order-page.component.scss
│       │   │           │   │
│       │   │           │   ├── widgets/
│       │   │           │   │   └── cleaning-order-form/
│       │   │           │   │       ├── cleaning-order-form.component.ts
│       │   │           │   │       ├── cleaning-order-form.component.html
│       │   │           │   │       ├── cleaning-order-form.component.scss
│       │   │           │   │       └── cleaning-order-form.model.ts
│       │   │           │   │
│       │   │           │   └── components/
│       │   │           │       ├── cleaning-order-summary/
│       │   │           │       │   ├── cleaning-order-summary.component.ts
│       │   │           │       │   ├── cleaning-order-summary.component.html
│       │   │           │       │   └── cleaning-order-summary.component.scss
│       │   │           │       └── cleaning-order-status-badge/
│       │   │           │           ├── cleaning-order-status-badge.component.ts
│       │   │           │           ├── cleaning-order-status-badge.component.html
│       │   │           │           └── cleaning-order-status-badge.component.scss
│       │   │           │
│       │   │           ├── domain/
│       │   │           │   ├── models/
│       │   │           │   │   └── cleaning-order.model.ts        # интерфейсы сущностей
│       │   │           │   ├── value-objects/
│       │   │           │   │   ├── address.vo.ts
│       │   │           │   │   └── money.vo.ts
│       │   │           │   ├── rules/
│       │   │           │   │   └── cleaning-order.rules.ts        # расчёты, проверки
│       │   │           │   ├── services/
│       │   │           │   │   └── cleaning-order.domain-service.ts
│       │   │           │   └── errors/
│       │   │           │       └── cleaning-order.errors.ts       # доменные ошибки/типы Result
│       │   │           │
│       │   │           ├── data-access/
│       │   │           │   ├── api/
│       │   │           │   │   ├── cleaning-order-api.service.ts  # HTTP-клиент фичи
│       │   │           │   │   └── cleaning-order.endpoints.ts    # baseUrl, пути, query-параметры
│       │   │           │   ├── dto/
│       │   │           │   │   └── cleaning-order.dto.ts          # контракты с backend
│       │   │           │   ├── mappers/
│       │   │           │   │   └── cleaning-order.mapper.ts       # DTO ↔ domain
│       │   │           │   └── mocks/
│       │   │           │       └── cleaning-order.mocks.ts        # заглушки, тестовые данные
│       │   │           │
│       │   │           ├── state/
│       │   │           │   ├── cleaning-order.state.ts            # интерфейс стейта и initialState
│       │   │           │   ├── cleaning-order.store.ts            # NgRx SignalStore
│       │   │           │   └── cleaning-order-facade.service.ts   # фасад фичи
│       │   │           │
│       │   │           └── testing/
│       │   │               ├── cleaning-order.fixtures.ts         # фикстуры для тестов
│       │   │               └── cleaning-order.spec.ts             # интеграционные/юнит тесты
│       │   │
│       │   └── subscription/            # другая фича домена cleaning (по тому же шаблону)
│       │       └── ...
│       │
│       └── users/                       # другой домен
│           ├── profile/
│           │   ├── project.json
│           │   └── src/
│           │       └── lib/
│           │           ├── user-profile.routes.ts
│           │           ├── ui/
│           │           │   ├── pages/user-profile-page/...
│           │           │   └── components/...
│           │           ├── domain/
│           │           ├── data-access/
│           │           ├── state/
│           │           └── testing/
│           └── auth/
│               └── ...                  # логин/регистрация, примерно тот же шаблон

└── tools/                               # опционально: скрипты, генераторы
    ├── generators/
    │   └── feature/
    │       └── index.ts                 # твой кастомный генератор "сделай фичу по шаблону"
    └── scripts/
        └── migrate-*.ts
```

---

Если коротко:

* **apps/** — только shell и верхнеуровневые маршруты.
* **libs/shared/** — маленькие переиспользуемые кирпичи без доменной логики.
* **libs/domains/** — мясо: домены → внутри домена **фичи**, каждая по нашему шаблону `ui + domain + data-access + state + testing`.

Дальше можно:

* либо сделать **CLI-генератор «новой фичи»** под эту структуру (в `tools/generators/feature`),
* либо зафиксировать это как официальный «архитектурный стандарт» проекта и от него уже писать доку.
